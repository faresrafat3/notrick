# VERSION — v0.0001
> stamped 2026-09-06 · moved to v0.0001 2026-09-19 — Owner line (verbatim: "اعتمد pr-024: A C D — وانقل الإصدار إلى v0.0001"), logged in the CONTEXT decision log · the version IS the distance-to-zero scale.

## Reading the version
`v0.0000N` — N = number of gates still locked before Zero day. More zeros = earlier.
Each gate that opens consumes one trailing zero (the number gets bigger as we approach 0.1):

- **v0.00001** — 5 gates locked: Owner signature · smoke call · threshold ·
  breakthrough definition · first falsifiable run (RJ-001 cond.1) — PASSED: signature
  landed 2026-09-06 (RATIFICATION-BATCH-001.md:13-15); version moved past it 2026-09-19
- **v0.0001 (NOW)** — signature landed (charters ratified); the doctrine is legally alive
  — REACHED 2026-09-19 by the Owner's line above
- **v0.001** — fuel proven (smoke call passed); the body can speak
- **v0.01** — first real task ran with pre-registered hypotheses (H1..H5) and a landed
  result record + K-line; threshold has its first value
- **v0.1** — Zero day complete: fate decisions made from results (expand/refine/retire);
  the system is now versioned by its own outcomes, not by gate-count
- **v1.0** — deliberately NOT defined by us. v1.0 is what the RESULTS say it is —
  per the doctrine: results are the only prices. Defining it now would be a noble lie.

## Rule
- The version NEVER moves backward. If a gate re-locks (a ratified charter is re-forged,
  a hypothesis fails hard), the version stays and the setback is recorded in the
  decision log — regression hiding is a noble lie (banned).
- Only the Owner moves the version. The agent may PROPOSE a move with evidence.