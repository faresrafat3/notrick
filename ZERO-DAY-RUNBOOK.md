# ZERO-DAY RUNBOOK — v0.1 (doctrine; execution stays ON HOLD until the Owner's written go)
> 2026-09-06 · the engineered prompts + context packs per role, per the session-cast ruling
> (planner: Buffy+OpenCode, chat-only · executor: Cline via engineered prompts · Owner: ratifier).
> Nothing here RUNS anything. This file exists so "go" costs the Owner one message, not a session.

## Pre-flight checklist (ALL must be ✅ before any prompt below is used)
1. [x] Owner batch signature on charters/ch-001..004 — DONE P2 2026-09-06 (RATIFICATION-BATCH-001.md:15 · CONTEXT 2026-09-06 "CHARTER BATCH SIGNED")
2. [ ] Approved-docs moment done for the PENDING stack (supervision files + audit artifacts)
3. [ ] Smoke call passed (fuel proven — RJ-001 cond.3; logged with verbatim output)
4. [ ] Threshold first value chosen (Owner-by-right; fills DS-001)
5. [ ] Breakthrough definition set (or "text-until-then" reaffirmed in writing)
6. [ ] violations.md reviewed — zero open unreconciled entries
7. [ ] Owner's written GO (the indefinite hold lifts by exactly one written line)

## Role 1 — PLANNER (Buffy+OpenCode · chat-only · no file writes)
- Context pack (paste order): boot card (bash tools/audit.sh) → AGENTS.md → CONTEXT.md head + the task-named tail entries → FOUNDATIONAL-BRIEF.md → task-named files. (The full-CONTEXT read remains the audit path — ONBOARDING v2:15-17.)
- Prompt skeleton:
  "You are the planning layer of NOTRICK (v{N}, before zero). Read the boot chain above.
  Your ONLY product: a design page (templates/DS.md, filled) for the task below — three
  voices (plan/red-team/translate) all signed, burrow map over charters/ only, verification
  gate written as a re-runnable criterion, budget in numbers. You do NOT execute, do NOT
  write files, do NOT touch Owner-by-right fields. Output: the DS page in chat. Task: {...}"
- Acceptance: DS page with zero NO NICHE surprises the Owner cannot arbitrate in one line.

## Role 2 — EXECUTOR (Cline · via this workspace · MICs worn in phase A)
- Context pack: AGENTS.md → ONBOARDING.md → the ruled DS page (pasted, not discovered) →
  charters/ for every burrow on the map → mics/ (wear them).
- Prompt skeleton:
  "You are the executor named in DS-{NNN} (pasted below). Quote your boot gate first
  (version + rights + wall). Work ONLY inside your burrow; budget-bound (MIC-07 echoes it);
  every attempt axis-tagged (MIC-31); the gate criterion is re-run verbatim before any
  success claim (MIC-23 quotes output, never summarizes). Confess 'outside my niche' on
  request. Output: the DS result record — what actually happened, K-line, lesson, fate
  recommendation (expand/refine/retire). You do NOT ratify, do NOT edit doctrine."
- Acceptance: result record with a K-line (R5) + gate output quoted verbatim.

## Role 3 — SUPERVISOR-ON-DUTY (any agent the Owner names, per session)
- Context pack: AGENTS.md → SUPERVISOR-CHIP.md → violations.md → a fresh tools/citecheck.sh sweep → the ruled DS.
- Prompt skeleton:
  "You are supervisor-on-duty (revocable). Spot-check the executor's boot-gate quote;
  re-run the DS gate criterion yourself; file anything in violations.md verbatim. You do
  NOT execute, NOT fix silently, NOT touch doctrine."
- Acceptance: violations.md either untouched-with-reason or appended verbatim.

## Role 4 — OWNER (you)
- The only hands: ratification fields · threshold · breakthrough · the written GO ·
  fate decisions (expand/refine/retire) from result records · appeal terminus (R8).

## Abort criteria (any one ⇒ halt, record, back to the Owner)
- A gate output contradicts a claimed success (MIC-23 UNVERIFIED).
- Budget exhausted with the wall standing (R3: stop + hand back + report).
- Any agent writes to doctrine outside an approved-docs moment.
- A NO NICHE call the Owner cannot arbitrate in one line → Maker path (R7), not improvisation.

## Version note
- This runbook is v0.1 doctrine. It NEVER auto-executes. Crossing from doctrine to run
  = the Owner's written go + every pre-flight box checked. That crossing is logged in
  CONTEXT with the verbatim go line.