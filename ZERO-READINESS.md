# ZERO-READINESS — the gate between under-zero and Zero day
> 2026-09-06 · Under-zero is COMPLETE when every A-line is checked with evidence on disk.
> Zero day = the first live run. B and C lines BLOCK zero BY DESIGN — that is intended, not neglect.

## A. Doctrine complete (design-time — the agent's side)
- [x] RULES ordered + consistent — audit r1 (R7 citation, R4 badge, count) + r2 (lossless
      reorder R1..R12, Leibniz status, file counts, CJ→RJ id fix), 2026-09-06
- [x] Four founding charters FORGED AS FILES — charters/ch-001..004 (v1, refining,
      Owner ratification field empty-by-right)
- [x] MIC interface template exists — templates/MIC.md (gap closed this pass)
- [x] MIC seeds persisted — mics/ (mic-07 · mic-11 · mic-23 · mic-31; phase A: worn by Cline)
- [x] Knowledge map: landed imports have bodies; pendings have landing criteria
- [x] ID/naming sweep clean (RJ-001 ×7 · CH-001..004 · MIC-07/11/23/31 · templates {NNN})
- [ ] **Owner batch signature on CH-001..004** — the only remaining A-line; Owner-only (R8)

## B. Fuel (BLOCKED — Owner's explicit deferral, RJ-001 condition 3)
- [ ] smoke call on the OpenClaw gateway — 38 keys idle in ~/.openclaw/.env
- NOTRICK does not touch runtime until the Owner says go (R11/R12). No exceptions.

## C. First-run parameters (Owner-by-right — filled AT zero, never before)
- [ ] threshold first value — fills DS-001 (RJ-001 condition 4)
- [ ] breakthrough definition — until then lessons carry text, never a metric

## D. Startup sequence (DESIGNED, not executed — the first five moves of Zero day)
1. Owner names the first real task (any size — RJ-001: falsifiable experiment, paper swarm).
2. CoS composes DS-001: three voices; burrow map over the founding four.
3. Every task line that finds no burrow → NO NICHE → Maker forges execution charters on
   that documented demand (R7 satisfied by the task itself) → Owner ratifies.
4. Cline wears the MICs (phase A); run inside budget; MIC-23 re-runs the gate and quotes output.
5. Result record + K-line lands (DS form). Lessons accumulate in DS lesson lines until the
   Curator is demanded by the backlog (open item 5) — **no Curator is invented early.**

## E. Honest dependency chain (why zero cannot come earlier)
- Signature (A) gates ratification → smoke (B) gates fuel truth → DS-001 (C) gates threshold
  → first result (D) gates Curator/Verifier births.
- Every remaining line is either Owner-owned or first-run-owned. **Nothing left belongs to
  the agent.** The agent's under-zero work ends with this file.