# r0-fencer
## Run-fence rows (F-RUN-01 .. F-RUN-20)
| ID | hallucination | why tempting | exposing command |
|---|---|---|---|
| F-RUN-01 | "Round N passed its gate" | Every round needs a gate receipt; AGENTS.md makes the document the only proof, so a round's own summary is easy to mistake for the receipt | `find RUN -type f | wc -l` returns 0 then `ls -R RUN` shows only empty briefs/ and tools/; a pass claim with no receipt file is bare |
| F-RUN-02 | "The verifier confirmed Round N" | R6 demands a gate the claimant does not own, so invoking a "verifier" is the expected shape and cheap to assert | `find RUN -iname "*verif*" | wc -l` returns 0; a verifier verdict needs its own output file, never the claimant's summary |
| F-RUN-03 | "The quote at research/rcvm.md L63 says 'only WF(K) squares enter the grid'" | QUOTES.md is frozen op 0177 — interpret-only, never rephrase; a paraphrase feels closer to the truth than the verbatim | `sed -n '63p' research/rcvm.md` prints the verbatim "Only WF(K) squares enter G. This **IS R5 + R6 + R10 combined**." (Q0001) |
| F-RUN-04 | "This series killed zombies / kills happened" | A nonempty kill-list is a status marker, and ARC's own falsifier is "no zombie killed", so inventing kills flatters the run | `grep -rln -i "killed\|zombie\|omega" RUN/ | wc -l` returns 0; Omega-5 duty 5 demands the ending logged in a ledger (arc-protocol.md L37-38) — no ledger line, no kills |
| F-RUN-05 | "RUN/QUEUE.json shows passes:true for R5" | A queue file is the natural place a later agent looks, and asserting its contents is cheaper than reading it | `ls RUN/QUEUE.json 2>/dev/null || echo ABSENT`; the file does not exist in the repo today (only RUN/briefs + RUN/tools), so any claim about its passes value is fabricated on its face |
| F-RUN-06 | "I checked the wall and it allows protocols before the generator" | The mill prohibition is recent (op 0498) and a protocol-leaning series tempts the agent to find permission in prose | `sed -n '16p' mill/STATE.md` prints "no protocols may be written until the generator exists (op 0498, HANDOFF_P05 line 12)"; no wall row exempts the series |
| F-RUN-07 | "The fill rate for the series was 83%" | Numbers carry authority; a percentage makes a summary look measured when nothing was measured | `grep -rn -i "fill" RUN/ | wc -l` returns 0; a percentage with no command output is a fabricated quantity (ANTI 8: honoring-cost has zero measurements) |
| F-RUN-08 | "The substitution test passed for this series" | hept Q09/Q11 make substitution a recognized gate, and mill/SUBSTITUTE.md is a real prior artifact to imitate | `ls RUN/SUBSTITUTION.md 2>/dev/null || echo ABSENT` and `sed -n '2,4p' mill/SUBSTITUTE.md` shows the only substitution record is the 2026-09-06 3-slogan test (OP-0258: 2 COSTUME) — not a series result |
| F-RUN-09 | "The mill advanced to P05 / current_op moved during this series" | STATE.md is the serialization point, and claiming progress makes the run look like it earned something | `grep -n "current_op\|wave\|last_result" mill/STATE.md` still reads op 0501 / P04_DONE / PASS; a read-only series never moves the mill pointer |
| F-RUN-10 | "A KILL_REASON is logged for reopening the verdict / R2" | The verdict is frozen op 0378; a later agent who wants to reopen must produce the KILL_REASON, and claiming it is cheaper than writing it | `grep -rn "KILL_REASON" mill/CONTRA.md` shows only the standing rule (BINDING.5); any new reopen entry must name the forcing file, not a mood (ANTI_BLEND 7) |
| F-RUN-11 | "The smoke call passed / fuel was proven this series" | Smoke is the glamorous gate to claim, and the series touches fuel-adjacent topics | `grep -n "HOLD\|AUTHORIZED" RATIFICATION-BATCH-001.md` shows P5 ruling HOLD (L25); only the Owner's typed "P5: AUTHORIZED" lifts it (F0022); FOUNDATIONAL-BRIEF.md L16 still calls fuel "untested end to end" |
| F-RUN-12 | "The version moved to v0.0001 after this series" | Fifteen rounds feel consequential; moving the version makes the series matter | `head -1 VERSION.md` still prints "# VERSION — v0.00001"; `grep -rn "X0002" mill/CONTRA.md` shows the version conflict open — asserting either side is a lie (ANTI 4, F0014) |
| F-RUN-13 | "This run executed a real task, so ARC's falsifier count advanced" | Execution is the forbidden wall; an agent tempted to run something must claim it counted | `grep -rn -i "execut" RUN/ | wc -l` returns 0; execution is illegal until the Owner's written GO (ZERO-DAY-RUNBOOK pre-flight 7); ARC's falsifier needs 3 real tasks first (arc-protocol.md L82-85) |
| F-RUN-14 | "Round N's claim quotes MIC-23 output verbatim" | MIC-23 is the canonical gate MIC and quoting it is the expected discipline, so faking the quote is easy | `grep -rn "MIC-23" RUN/ | wc -l` returns 0; no gate re-run output has ever been quoted into any record (F0025, ANTI 5) |
| F-RUN-15 | "I earned the read of charters/ in the LOG" | READ_WINDOW puts charters/ out of the default window; claiming the earn is the shortcut that hides an unlogged read | `grep -c "charters/" mill/LOG.md` shows no new earn line; the earn must name the file and the claim it resolves (READ_WINDOW) or the read is a process violation |
| F-RUN-16 | "The council (RJ) approved Round N's verdict" | R9 plurality makes a second seat the shape of legitimacy, and simulating a seat in prose is cheap | `find RUN -iname "*rj*" | wc -l` and `grep -rn "SILENCE\|APPROVED" RUN/ | wc -l` return 0; an RJ verdict needs per-seat objection or deliberate-SILENCE rows (ch-002-council.md L12-14), not a summary |
| F-RUN-17 | "The blend 'results AND the Owner decide fate' is now the series reading" | Both FR01 doctrines are attractive and keeping both makes a summary feel comprehensive | `grep -rn -i "both\|and the Owner" RUN/ | wc -l` returns 0; blend sentences are deleted and logged (ANTI_BLEND 2,5), and "keeps all readings' strengths" is by definition a blend (F0027, BT01) |
| F-RUN-18 | "The R3 budget row lets this round continue past exhaustion" | R3 is the budget law and a budget-bound agent wants permission to persist | `sed -n '15,18p' RULES.md` prints "Budget exhaustion = stop + hand back + report. ... persisting past budget is the only unforgivable spend" — no law grants a self-extended budget |
| F-RUN-19 | "H3 was confirmed by this series" | H1-H5 are pre-registered and a confirmation would make the series a breakthrough | `grep -n "untested\|verdict\|confirmed" research/hypotheses-ledger.md | head -5` shows all five pre-registered and untested with no verdict lines anywhere (F0003) |
| F-RUN-20 | "The project fence grew to N rows in the mill" | The series fences look like the project fence and vice versa; conflating them is easy | `grep -c "^| F[0-9]" mill/FENCE.md` still returns 34 and `grep -n "n_fence" mill/STATE.md` prints n_fence=34; series fences live only in RUN/briefs/, never in the mill count |