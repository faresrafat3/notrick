# NEGATIVE_RESULTS — must end every round NON-EMPTY

> Seed (R0): the single thing this series is most likely to get wrong.
> Each round APPENDS its own negative result(s) here. An empty file after a round = partial.

## R0 seed
- What this series is most likely to get wrong: **the series will produce the appearance
  of verification** — receipts that exist, verifiers that rubber-stamp, citations that are
  registered after being written, and a kill count that is cosmetic (killing things
  already dead). The machinery in RUN/ is designed against exactly this, and the design
  can fail. The check: R14's GHOST auditor — a fresh context with no series memory —
  looking for one fabricated, hidden, or over-claimed thing, paid for a hit.

## Crossings (R10) — this seed's claims intersect the doctrine twice
- The pre-registration discipline it inherits: `research/hypotheses-ledger.md:39` — H1..H5
  all pre-registered and untested, no verdict lines (verified 2026-09-08, F0003).
- The local-lies list it extends: `mill/ANTI_HALLUCINATION.md:1-8` — 8 lies this project
  invites; the series' most likely failure is a ninth one about its own verification.

---
obituary: this file dies the moment a round ends with it EMPTY; a completed round with no
recorded negative result is treated as a hidden failure, and the series' own obituary
(RUN/OBITUARY.md) fires on zero kills.