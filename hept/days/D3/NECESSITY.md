# NECESSITY AUDIT (D3-Q10)

At most 5 N-NECESSARY items, each with an object-death test: if the item is removed, does the OBJECT (R1 verification-discipline) die?

| # | N-NECESSARY item | Death test (remove it — what happens?) | Verdict |
|---|------------------|----------------------------------------|---------|
| N1 | Frozen pre-registered criterion (BK3 + rejection condition) | Without it, every gate is self-owned; DS-P-001's catch becomes unexplainable; R6 is decoration. The doctrine's one lived proof dies. | NECESSARY — object dies |
| N2 | In-turn command output as the only lawful value (BK2) | Without it, the P14 fabrication class has no wall; the ledger lies at the value level; every downstream check reads poisoned inputs. | NECESSARY — object dies |
| N3 | UNVERIFIED as terminal flag (BK5 + propagation) | Without it, unverifiable claims are silently funded or silently deleted; the diagnosis loop that caught V-001/V-002 is cut. | NECESSARY — object dies |
| N4 | Verification pricing (BK4) | Without it, checking is either unbounded or absent (both observed). Object survives but cannot scale past one rehearsal — wounded, not dead. | CONDITIONAL — keep, flagged by D0021 (finiteness premise unmeasured) |
| N5 | The refusal of retro-logging as a doctrine channel (HATCH 2, VERDICT loss #1 scope) | Without it, any write is legalizable after the fact, which re-opens R3's program (process over proof) — the exact rival scored 6/20. | NECESSARY — object's boundary dies (it becomes R3 with better vocabulary) |

## Fake destinies demoted (were candidates, failed the death test)
| Candidate | Why demoted |
|-----------|-------------|
| "Documents are the only voice" | Object survives without it (VERDICT loss #2) — it was the OLD object's destiny, not this one's. |
| "No live orchestrator" as achieved state | Object survives; the claim is untested (D0005) — a destiny claimed without a run. |
| The version register as distance-to-zero | Object survives; register measured nothing (D0008) — a destiny of the dead-weight list, not of verification. |
| RCVM as substrate | Object survives on records alone (VERDICT loss #4) — formalism is optional schema material. |
| "Results are the only prices" as slogan | Object needs PRICED CHECKS (N4), not result-priced fates — the slogan overreached what the object requires. |

## Count
5 N-NECESSARY items (1 conditional), 5 destinies demoted. Object = "refuse every success claim that cannot re-run its own test" (NICHE_CUT).