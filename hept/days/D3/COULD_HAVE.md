# COULD-HAVE THEORY (D3-Q13)

Keep ALL five N-NECESSARY items (N1-N5). Change the contingent packaging. Question: does a cheaper contingent packaging do the same work?

## The could-have variant: "Pre-registration with receipts, no charters, no VM, no queue"
- Keep N1 (frozen criterion) as: a two-line header in the attempt file itself — `criterion:` + `would-fail-if:` — no charter, no gate role.
- Keep N2 (in-turn output) as: a paste-hygiene rule — `receipt:` line must contain the verbatim command string + output; no values typed from memory.
- Keep N3 (UNVERIFIED terminal + propagation) as: one ledger column in the same attempt file; descendants inherit.
- Keep N4 (pricing) as: one integer in the header — `price: <steps>` chosen before running.
- Keep N5 (no retro-legalization) as: append-only; edits to judged files are new files citing ancestors (already the pr-proposal pattern).

## Does the could-have match the verdict's object?
- Refuse unrerunnable success claims: YES — every N-item survives without charters (CH-001..004), without the RCVM, without the Owner queue, without the version register.
- Work the current corpus does that the could-have CANNOT do: none found. Charters assign burrows; but the object never needed burrow assignment — V-001/V-002 and DS-P-001 were caught by criterion/receipt mechanics, not by charter powers.
- What the could-have LOSES: role separation (Maker/CoS/Council identities), Kant/Gause ceremony, the "four agencies" narrative. None of these is load-bearing under R1 (VERDICT loss #3 already demoted them).

## Novelty downgrade (recorded)
- The STRICT theory (R1 applied) is: **a file format** — header with criterion/would-fail-if/price + receipt + UNVERIFIED flag, append-only with ancestor citation. Everything else in the 40-file corpus is contingent scaffolding.
- Therefore: the workspace's novel content is NOT the charter architecture, NOT the VM, NOT the governance mythology. The candidate novelty is the five-field discipline itself. Downgraded accordingly: any claim of "systemic innovation" beyond the five fields is unearned (L6 guilty-until-earned).