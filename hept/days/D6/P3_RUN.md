# P3 RUN — LEGAL-MOVE GENERATION (D6-Q04)

**State chosen (real):** `research/arc-protocol.md`'s pre-registered obituary claim — "zero catches in 3 real tasks → retires by its own Omega-5" (already processed in RUN11 as UNRUNNABLE 0/3 tries; carries D0020).

## Step 1 — Generators applied
| Generator | Output |
|-----------|--------|
| G1 AUDIT-ONE | no trace to re-run (zero tries recorded) — no candidate |
| G2 CRITERION-CRITIC | criterion (obituary) names a fail condition; but the exposure class is UNDECLARED — "3 real tasks" without saying which tasks count. Candidate: declare the class or the count is denominated by nothing (X10). |
| G8 STALL-ACCOUNTANT | the claim has stalled in UNRUNNABLE since posting; invoice: cost = any PASS banked elsewhere in the lane can cite it as "pre-registered support" without it ever being exposable. Candidate: block citation-as-support from unexposed pre-registrations. |
| G7 FORK-TRIGGER | no attribution hole; no fire. |

## Step 2 — Filters (X1-X12, seven rules)
- Candidate 1 (declare exposure class): LEGAL — X10 demands a pre-declared class; the declaration is a rule-1 (criterion) completion, not a new mechanism.
- Candidate 2 (block citation-as-support): ILLEGAL as stated — it would add a new refusal rule (scope widening, NEGATIVE_DEPTH). Re-derived: the legal form already exists in Rule 4 inheritance — citing an UNRUNNABLE claim as support inherits its mark. So candidate 2 collapses INTO candidate 1's consequence: no new rule needed.

## Step 3 — Scoring (survivor = candidate 1)
depth 2 (names the hidden denominator condition), surprise 2 (forced by X10 on a real file), debt-pay 2 (D0020), coherence 3 (strengthens X10's first live application).

## Step 4 — Executed move
The move is recorded here as the P3 run's product (editing the research file itself is outside the hept/ write lane — guest mode, R-lane law):
**Required declaration for arc-protocol's obituary to become countable:** exposure class = "NOTRICK tasks where arc-protocol's boundary check is actually invoked and its result recorded in the task's own record". Until declared, its count stays 0/3 and its mark propagates (Rule 4) to anything citing it.
- Anchors: research/arc-protocol.md obituary (pre-registered falsifier) · X10 · D0020 · RUN11.

## Step 5 — Outcome
One legal move executed (declaration requirement stated with anchors). Zero-legal-move fallback not needed. The instruction to a future agent (D6-Q14) will carry this declaration as a candidate standing order.