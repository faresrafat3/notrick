# COMPRESS (D6-Q15)

Compressed carriers of the theory's four load-bearing pieces. Padding deleted. These are what CANON quotes.

## VERDICT (compressed from D3 VERDICT + D5 changes)
R1 chosen: the work is a machine for refusing unverifiable success claims. Lost: attribution-object, documents-as-voice, charters-as-center, RCVM-substrate, queue-as-verdict. Survived: R1 as 7 rules; R2 and S2 as live forks (F-S2, F-R2) with named experiments. Everything else killed or tooling.

## PRIMITIVES (compressed; bland names final)
RECORD (judged append-only unit), CRITERION (frozen test + fail condition + class), TRACE (timestamped paste + determinism class), MARK (exact gap-set, terminal, inheriting), PRICE (committed cost pair), CLASS (pre-declared exposure/exposure class). Six. No others.

## GENERATOR (compressed)
States in, moves out: G1 audit-one (re-run trace), G2 criterion-critic (fail condition + reachability), G3 chain-walk (laundering), G4 catch-banker (retire on counted zero), G5 price-audit (pre-commit), G6 ancestor-hole (citation demand), G7 fork-trigger (paste-insufficiency only), G8 stall-accountant (invoices, not verdicts). Filters: X1-X12, seven rules. Scored; sounding-smart = 0.

## NICHE (compressed, 12 words)
Refuse every success claim that cannot re-run its own test in front of you.

## Density check (P6 assemble-increment)
- 4 carriers, each ≤1 screen; every sentence anchored in prior artifacts (VERDICT→D3/D5; primitives→D4+P1/P4 amendments; generator→D4 Q10 + X-updates; niche→D3 Q06).
- Nothing deleted that had a live edge in DEP_GRAPH; nothing kept that lacked one.
- Verdict: stricter-and-shorter than the D4/D5 sources it compresses. COMMITTED as CANON draft material.