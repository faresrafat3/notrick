# NEXT DEEPEST MOVE (D6-Q14)

## The one instruction (to the next agent, executable as written)

**Open a record for the arc-protocol obituary (research/arc-protocol.md): declare its exposure class as "NOTRICK tasks where the arc boundary check is invoked and its result is recorded in the task record", then run it on the next 3 real tasks — banking COUNT for every invocation, and letting its pre-registered retirement (zero catches in 3 real tasks → Omega-5) execute mechanically if it fires. Append each invocation as a record with criterion/trace/cost/mark/environment-class per the seven rules, citing the obituary as ancestor.**

## Why this move (and not another)
- It is the first full seven-rule cycle on a live corpus claim (currently stuck at UNRUNNABLE 0/3, D0020; P3_RUN declared its class requirement).
- It pays the top debts at once: D0020 (unrunnable pre-registrations), D0009 (unverified live state), and the research lane's zero-PASS domain (P1-RUN C5).
- It exercises NC3's first real retirement path (the obituary's Omega-5 is the corpus's only pre-registered kill switch) — Rule 6's convergence finally gets a testable instance.
- It is executable without session knowledge: the file, the falsifier, and the class are all on disk.

## Stranger-test (executed)
A fresh reader with only hept/CANON.md (once written) + the files cited above can:
1. Find the obituary clause (research/arc-protocol.md, pre-registered falsifier) — verifiable, it is quoted in RUN11 and P3_RUN.
2. Understand "exposure class declaration" from X10's definition (INVARIANTS/EXPERT) — the P3_RUN states the exact class string to use.
3. Execute "next 3 real tasks" — the instruction names the trigger condition; the agent does not need to know what arc-protocol is beyond its record header, because the record carries the check.
4. Know what to do on zero-catches: let the pre-registered retirement fire — no discretion involved (HATCH-3 bans informal keeping).
**Result: PASS** — every step grounds to a path; no step requires this session's memory. The one dependency is that CANON.md (Day 7) must contain Rule 1-7 and the class declaration string; NEXT_MOVE is therefore also the acceptance test for CANON section 15.

## Not chosen (and why, briefly)
- "Run the multi-writer experiment" — bigger, but it requires an orchestration surface the workspace does not have (no second writer surface exists; the experiment is NC5's, with its own standing order in the fork table).
- "Bank the first seven-rule PASS on a practice file" — tempting, but no practice claim currently has a runnable criterion; manufacturing one for the sake of a PASS would be the doctrine's own theater (RUN01 kill class).