# EX1 — REAL FRAGMENT IN THE SKELETON (D4-Q07)

Fragment: V-001, the fabricated hash pin (violations.md:5).

## The incident in the schema
| Field | Value in the incident |
|-------|----------------------|
| C (criterion) | ABSENT — "integrity check" existed as a habit, but no frozen test text with a named fail condition was attached to the specific write |
| R (receipt) | the value `f0a9b3c2d1e4…f9a0` was TYPED, not pasted — no command output behind it |
| K (cost) | the check cost was never committed; the pin write was priced at zero |
| M (mark) | the write carried an implicit PASS with no run behind it |
| F (fork) | none — the incident was treated as isolated, not as a rival theory's prediction |

## What the machine does with it
1. The RECORD (INTEGRITY.md edit) attempted to bank a PASS (pin recorded) without R — operation O3 fails-when fires: value without trace line.
2. Correct machine outcome: O4 MARK = UNVERIFIED, gap named = "no in-turn sha256sum output for pr-009".
3. The actual self-cure (real hash computed in-turn, 0-mismatch verify) is a legitimate ancestor-citing amendment (R6, O6-adjacent): new RECORD citing V-001, MARK cleared because the gap (missing trace) was closed by a real paste.
4. COUNT for the P14 wall: exposure = 2 (V-001, V-002), catches = 2 (both caught) — the wall banks CATCHES, D5.

## What the skeleton changes in the retelling
- In the original corpus, the cure is narrated as diligence ("caught in same-turn self-review"). In the skeleton, the cure is a state transition with a named gap — nothing about diligence or virtue remains.
- The catch is attributable to the mechanism (P14 wall = "no value without paste"), not to the person checking themselves.

## Residue check
- No fragment concept lacks a schema home: fabrication = R-violation; self-catch = O4 gap-close; pattern = COUNT/CATCHES. Zero residue — EX1 fits.