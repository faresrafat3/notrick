# SEALED NON-AUTHORITY ZONES

Status: SEALED AS LAW
Date: 2026-09-06
Rule: Raw text only. Zero authority.

## Sealed Paths
- `mill/`
- `well/` (if present)
- `phantom/` (if present)

## Legal Consequences
1. Any canon, summary, synthesis, or rubric inside sealed directories has zero governing authority.
2. Titles, abstracts, mission statements, and reading orders inside sealed directories are non-binding.
3. Raw workspace sources (e.g. `research/`, `charters/`, `mics/`, `proposals/`, code/configs) outrank any claim made inside sealed paths.
4. Sealed texts may be mined for raw strings, quotes, or historical artifacts, but cannot serve as proof, precedent, or justification.

[Dated extension, 2026-09-09, D7-R17 — anchor repair class, original rule untouched:
the workspace has grown lanes this file's D1 snapshot did not name. Current standing:
- `bore/` — a sibling foundry lane (own days/, ledger/, GRAVEYARD, STANDING_ORDERS; CORE v1.2 frozen; its site claims verified 3× byte-exact per its own LOG). Treated as RAW SOURCE material: its verbatim quotes and receipt-backed arithmetic are bindable as evidence (Q0036-Q0037 precedent), its doctrine is non-authority for this lane.
- `RUN/` — the series-2 verification machinery (receipts, LEDGER, WALL, OBITUARY, GROUND_TRUTH). Treated as RAW SOURCE with one sharpened property: its receipts are command-backed task records and therefore lawful ANCHORS for workspace facts (D7-R08 precedent) — but its verdicts about hept/ doctrine remain non-binding; only this lane's own ledgers judge hept/ claims.
- `well/`, `mill/`, `phantom/` — remain sealed exactly as above; well/'s and mill/'s outputs are readable raw text, never law (their own LANE laws do not cross into hept/).
Consequence: the sealed set did not shrink; the raw-source class gained two named members. Every binding from them keeps the non-authority rule: quotes yes, precedent no.]
