# BECAUSE LEDGER — PREFLIGHT (D2-Q14)

Kills circular becauses on 4 load-bearing claims.

| ID | Claim | BECAUSE (contrastive) | INSTEAD-OF (strongest rival) | IF-FALSE (named collapse) |
|----|-------|-----------------------|------------------------------|---------------------------|
| BC1 | C0016: Any claimed success passes a gate it does not own. | Because a self-owned gate cannot distinguish a true success from a rehearsed one — the claimer controls the criterion, so a PASS proves only compliance with a text the claimer chose; contrast: a frozen pre-registered criterion selected by a different role (HATCH 3) keeps at least one degree of adversarial distance. | INSTEAD-OF: "verification is a social act" (R9 plurality — witnesses make it public) — plausible, but witnesses without a frozen criterion can still be gamed by shared narrative. | IF-FALSE, the rehearsal DS-P-001's catch (gate failed the maker's own tagline) becomes a coincidence, not a mechanism; R6 collapses into "verify you agree with yourself". |
| BC2 | C0007: nobody prices the checking itself. | Because cost(verify) = cost(execute) at full re-run bankrupts fuel before a 5-run quorum; pricing makes the checker an accounted actor instead of a free rider — contrast: receipts make verification a first-class budget line. | INSTEAD-OF: "trust the checker" (honesty as character) — cheaper but falsified by V-001/V-002, where the checker was the fabricator. | IF-FALSE, verification is unmeasured and unlimited; the fuel-bankruptcy bound (pr-005) is the collapse the claim prevents. |
| BC3 | C0003: unrunnable claims are flagged UNVERIFIED. | Because a claimed verdict with no re-runnable criterion is an unearned therefore — flagging it keeps the record honest; contrast: merely ignoring it lets false verdicts sit as facts. | INSTEAD-OF: "assume good faith until proven otherwise" — cheaper socially, but treats absence of evidence as presence of safety, exactly the P14 failure mode. | IF-FALSE, any unverifiable claim can stand; the ledger becomes a fiction repository. |
| BC4 | C0015: K = (A, D, W, G) as the instruction set. | Because a fixed 4-tuple makes every transition testable: each square must name charters, axis, wall, and gate outcome — contrast: untyped records cannot be checked for completeness or reuse. | INSTEAD-OF: "free-text result records" — simpler, but they make R5's "indexable" unenforceable and cross-task SCAN impossible. | IF-FALSE, indexing and axis reuse (H5) lose their formal footing; the K-line becomes decoration (D0002). |

## Circular-because kills
- BC1 kills "a gate is any check, this check is a gate" (self-referential definition).
- BC2 kills "receipts matter because receipted tasks matter" (no external ground).
- BC3 kills "UNVERIFIED is right because the rule says flag" (restates itself).
- BC4 kills "K-lines are the instruction set because instruction sets are K-lines" (circular taxonomy).
Each row now carries a contrastive BECAUSE, a named rival, and a named collapse.

# KEEP LEDGER — D3-Q07 (8 KEEP claims under VERDICT R1)

| ID | KEEP claim | BECAUSE (contrastive) | INSTEAD-OF (strongest rival) | IF-FALSE (named collapse) |
|----|-----------|-----------------------|------------------------------|---------------------------|
| BK1 | A success claim without a re-runnable criterion is not a success (C0003). | Because a claim that cannot be re-run cannot lose — it wins by default against every future test; contrast: a frozen criterion can fail, which is what makes passing worth anything. | INSTEAD-OF: adjudicate claims case-by-case by human judgment — flexible but re-introduces the claimant-as-judge (F12) with extra steps. | IF-FALSE, the corpus's only real catch (DS-P-001 gate failure) is unexplainable; the doctrine loses its one lived proof. |
| BK2 | Values in records must be in-turn command output (P14 cure). | Because both logged fabrications (V-001/V-002) have the identical root: a plausible value typed where only command output is lawful; contrast: retyped values are the exact production mechanism of the failure we measured twice. | INSTEAD-OF: careful self-review before writing — this was the standing cure and it failed both times it mattered (V-001 notes: worked "only when self-review ran it"). | IF-FALSE, the ledger is trusted on the same mechanism that already lied twice; integrity-pinning collapses into theater (K8). |
| BK3 | The criterion must be frozen BEFORE the attempt (HATCH 3 closed form). | Because a criterion selected after seeing the attempt is fitted to it — the test then confirms the attempt by construction; contrast: pre-frozen criteria are the only form that can produce a genuine FAIL, and a FAIL is the only output that distinguishes a test from a ritual. | INSTEAD-OF: post-hoc adversarial review — stronger reviewer, but it cannot recover the frozen-before fact, and DS-P-001 shows post-hoc is where friendly grading lives. | IF-FALSE, gates stop being able to fail the maker; R6 becomes unfalsifiable decoration. |
| BK4 | Verification must be priced before it runs (C0007). | Because an unpriced check grows without bound or never runs at all — both are observed here (full re-run bankruptcy vs nobody checks); contrast: a priced check is a budget line that someone must actually pay. | INSTEAD-OF: check everything exhaustively — impossible under the fuel bound pr-005 documents (cost(verify)=cost(execute) at swarm scale). | IF-FALSE, verification economics vanish and the checking cost hole that pr-005 named stays open forever. |
| BK5 | UNVERIFIED is a terminal state, not a shame state (C0003's flag semantics). | Because the alternative — deleting or shaming unverifiable claims — teaches agents to hide them (noble lie, banned); contrast: a terminal flag keeps the claim visible AND unfunded. | INSTEAD-OF: reject unverifiable claims outright — cleaner ledger but destroys the evidence trail that V-001/V-002 needed to diagnose P14. | IF-FALSE, unverifiable claims get suppressed instead of flagged, and the fabrication diagnosis loop (which worked twice) is cut. |
| BK6 | Receipts are EVIDENCE, never TRUTH (pr-005 R-4 direction, kept under R1). | Because a receipt proves only that a command ran and what it printed — the inference to "success" is a separate act owned by the criterion; contrast: receipts-as-truth re-creates the claimant-owned gate one level down. | INSTEAD-OF: no receipts, trust narratives — the exact practice that produced zero-priced fates (D0007). | IF-FALSE, hash-matching becomes a success oracle and a forged receipt equals a passed gate (pr-006's attack annex). |
| BK7 | A mechanism with zero catches in its life is demoted, not defended. | Because mechanisms here earn by catching (MIC-23's metric is catches/false-flags); contrast: mechanisms that survive on design elegance are the dead weight D2 found ten of. | INSTEAD-OF: keep mechanisms for future use — "future-useful" is an illegal reason (L9); unearned futures are how 40 files of costume accrued. | IF-FALSE, the corpus only grows; every graveyard entry (D2 DEAD_WEIGHT rows) would have to be un-killed, and L11 breaks. |
| BK8 | The verdict may lose flagship claims when evidence forces it (VERDICT's five losses). | Because keeping flagship language that practice contradicts is the precise anti-clone failure (L17) — the doctrine would be the title with darker adjectives; contrast: losing "documents are the only voice" buys a doctrine that matches what actually fired. | INSTEAD-OF: preserve flagship claims and patch practice to match — this is R3's program (process over proof), scored 6/20 and killed. | IF-FALSE, the doctrine re-attaches to its slogans, and every D2 fracture re-opens as smooth consistency theater. |