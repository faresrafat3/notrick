# Research note — the provider subcontract-exchange (concept, NOT built)
> Persisted 2026-09-06 so the idea survives outside chat memory (CONTEXT flagged it "no file yet").

## Origin (verified, DSH sessions)
- Key pools: 11 OpenRouter + 10 Cline + 10 OpenCode + 3 AgentRouter; rotation skills
  (agentrouter-pool, smart-pool) already proven in DSH.
- Swarm reality: parallel agents multiply request volume — a single key rate-limits.
  Rate limits kill swarms before intelligence does.

## Concept (design only)
- One OpenAI-compatible endpoint in front of the key pool.
- Rotation across keys; per-niche budget accounting (feeds R3 axis ledger).
- The exchange is FUEL plumbing: R11/R12 place it in runtime, never in doctrine.

## Status
- **NOT BUILT. Nothing here may be claimed working.**
- Landing criterion: the first real run that actually rate-limits (demand-documented,
  per the R7 spirit — no speculative building before zero).