# THE ONE OBJECT — anti-blend verdict · R10 · SERIES 2 (2026-09-08)

> Executor: SERIES 2 R10 lane (glm-5.3-flash session). Law applied: mill/ANTI_BLEND.md
> (1: ONE doctrine, ONE object, concrete discards with IDs; 4: a verdict that loses nothing
> is fake; 5: keeping all readings' strengths = blend = deleted; 8: auditor reviews blend
> syntax before freeze). Standing vocabulary: mill/BLEND_TEMPTATION.md BT01-BT06.
> W7 honored: nothing killed below reopens a frozen verdict without KILL_REASON from files.

## The verdict (one sentence)
NOTRICK's one object is **the 6-field STATE_SCHEMA row** (`ENTRY.id · QUOTE · GATE · RECEIPT ·
SLOTS · VERDICT`, mill/STATE_SCHEMA.md:3-9) — everything else in the repo is a row, a
predicate over rows, a production of rows, or a decoration the row exposes.

## Why this object and not its rivals (each refusal concrete, per ANTI_BLEND:1)
1. **Not the K-line 4-tuple** K=(A,D,W,G) (research/rcvm.md:48-58): the K-line is a
   PRODUCTION over rows — "every valid state transition is a K-line production" (rcvm.md:44)
   — a transition is not an object at rest; the machine consumes rows and only rows
   (mill/HANDOFF_P05.md:4: "sets of 6-field rows; no other structure is legal"). The
   K-line survives as the row-sequence grammar, demoted from object to operation.
2. **Not the crossword** (C0001): already killed by the mill verdict — mill/LOST.md:5 "the
   grid is a ledger, not a puzzle of mind"; W7 keeps it dead.
3. **Not the VM/opcode machine** (C0003, FR04): already killed (mill/LOST.md:6, "the machine
   interpretation is closed"); the P05 designs inherit this — the emitter "composes and
   counts; it never interprets" (p05-generator-machine.md:19-21).
4. **Not results-as-prices** (C0007, LB04): killed in W03 (well/KILLED_WHYS.md, R4's
   claim-level kill); the price metaphor has no buyer (mill/LOST.md:2).
5. **Not stigmergy** (C0008): killed — "documents ARE entries", not environment
   (mill/LOST.md:3); the one object makes this literal: a document IS rows.
6. **Not the growing mind** (LOST.md:1): "NOTRICK is now a bookkeeping discipline" — the
   one object is the discipline's unit of account.

## The reduction table (what reduces to the row — each anchor quoted on disk)
| Artifact | Reduces to | Anchor |
|---|---|---|
| Receipts (RUN/receipts/) | rows with VERDICT=token + RECEIPT bytes | HANDOFF_P05.md:4-5 |
| Gate objects (P05 part I) | predicates over frozen rows (5 fields, inputs=row digests) | p05-executable-gate-spec.md fields 2-3 |
| The emitter's protocol files | rendered rows (stages 1-6 compose/only row content) | p05-generator-machine.md:23-34 |
| K-lines | productions over rows | rcvm.md:44 |
| Violations, proposals, LEDGER | rows with empty-by-right or Owner-typed fields | violations.md:5,7 form |
| The crossword grid | rows indexed by (axis, wall) — SCAN/THEME are row queries | rcvm.md:42-44 |

## What the verdict LOSES (per ANTI_BLEND:4 — a verdict that loses nothing is fake)
1. The K-line algebra loses object-status: rcvm.md's K=(A,D,W,G) is demoted to a derived
   view — the algebra's composition laws (∘ₐ, ∘𝒹) now describe row-sequence patterns, not
   a second kind of thing. That is a real loss of the design's elegance, paid knowingly.
2. The crossword reading loses even its "useful metaphor" status: if the grid is rows, the
   puzzle never existed — only the ledger did.
3. The row itself loses warmth: nothing in the 6 fields remembers that a human signed the
   charters; the Owner appears only as the outermost field's authority (ownerBind), never
   as a mind. The discipline is the residue; that is the verdict's price.

## Blend rows refused (BT vocabulary, standing)
- BT05 refused: this verdict does not "keep all four readings as modalities" — it kills
  four readings with IDs above and demotes one.
- BT06 refused: the engine is bookkeeping when talked about AND when audited — one reading,
  no context-switching.
- BT04 refused: charters ACTIVE LAW + zero-day blocked is not "both true in their own way";
  the signature lifted the charters into law and lifted no gate — one object (the row's
  GATE field) explains why: a law is a row, a GO is a different row's VERDICT token.

## Crossings (R10 — each claim intersects ≥2 existing entries)
STATE_SCHEMA:3-9 + HANDOFF_P05:3 · LOST.md:1-6 + BLEND_TEMPTATION BT01-BT06 ·
rcvm.md:44-58 + p05-generator-machine.md:23-34 · well/KILLED_WHYS.md + mill/LOST.md:2 ·
violations.md:5,7 + templates/PROPOSAL.md:18.
>
> obituary: this verdict dies the moment any repo artifact is shown to be legally
> neither a row, nor a predicate over rows, nor a production of rows (mill/HANDOFF_P05.md:4
> is the law that makes that showing decisive) — then a second object exists and this
> verdict was a blend by other means; it also dies if the R14 GHOST re-derives the
> reduction table and finds a row above misquoted — then the table, not the verdict, was
> the fake.