# UNPAID_WHY — empty table for reasons that failed the law.
> Format: `UWxx | target (claim/decision) | missing contrast (T-CONTRAST) | likely illegal reason kind`

| UWxx | target | missing contrast | likely illegal kind |
|------|--------|------------------|---------------------|
| UW01 | LB04 — positive form of "results are the prices" | T-CONTRAST fails: the fence has no positive rival named; we cannot say what it permits. | X-AUTHORITY (citing the fence as if its positive form were settled) / X-HOLISM (the fence reads as complete). Anchor: X0003 / CONTRA.md L6 open. |
| UW02 | LB07 — external wall for the dead orchestrator | T-COLLAPSE fails: the runner/judge distinction (rcvm.md L123) is self-attested (Q0014 / adversarial-audit.md L25). The next "why" cannot terminate at a non-self-attested wall because no such wall is named. | X-AUTHORITY (the wall exists *because the orchestrator says it does*). Anchor: X0004 / CONTRA.md L7 open. |
| UW03 | LB09 — same-task operator for niche-law falsifier | T-CONTRAST fails: the falsifier `≥3 same tasks` presupposes a same-task operator that D-01 L10-11 does not define. Without the operator, two tasks can never be "same enough", and the falsifier is unfalsifiable-by-construction. | X-CIRCULAR (the falsifier reduces to "if we say the tasks are the same, the falsifier fires"). Anchor: D-01 L10-11. |

## Status after W02
- W01 had 0 X-CIRCULAR catches; W02 found 3 ARBITRARY stops that are debts owed by the records, not by the agent's writing.
- All three UW entries correspond to the W01 DEMOTE-KEEPs (LB04, LB07, LB09) — the W01 reopen pass identified the right debts, and W02 traced each to its chain.
- W03 task: each UW entry needs either (a) the missing record-piece supplied by an Owner ruling / new source / falsifiable measurement, or (b) the claim DEMOTE'd fully to UNPAID (no longer load-bearing).

## How an entry is retired
- Re-attempt with the missing field filled; the retried because-clause lives in the matching
  LB-NN file (not here) and the UW row is annotated `→ retired, see LB-NN`.