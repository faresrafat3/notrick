# HANDOFF_W03 — the theory as a stack of stops, not a stack of nouns.
> Op 16 directive: hand off the theory as a *stack of stops*. W03's job is to test whether
> the load-bearing claims still survive when they are restated as stops, not as nouns.

## The 12 claims as stops (LB01..LB12)
1. **LB01**: stop at `R6 ∧ conjunction of four conjuncts` (rcvm.md L63).
2. **LB02**: stop at `R3 ∧ pre-registered kill` (arc-protocol.md L24-25).
3. **LB03**: stop at `R3 ∧ R8` — mortality + Owner terminus (arc-protocol.md L10-13, RULES.md L33-35).
4. **LB04**: stop at **arbitrary** — UW01 (positive form missing). Read as fence only.
5. **LB05**: stop at `R7 ∧ R10` — niche + crossword (decision-review.md D-01 L6-11).
6. **LB06**: stop at `R7` (RULES.md L28).
7. **LB07**: stop at **arbitrary** — UW02 (external wall missing). Read with CHL-WALLS-SELF-ATTESTED caveat (X0004).
8. **LB08**: stop at `R6 ∧ R3` — unowned gate + budget (RULES.md L25, L15-18).
9. **LB09**: stop at **arbitrary** — UW03 (same-task operator missing).
10. **LB10**: stop at `R7` (ch-003-maker.md L18).
11. **LB11**: stop at `R8 ∧ Owner line` (RATIFICATION-BATCH-001 L15).
12. **LB12**: stop at `R7 ∧ R12` — demand + doctrine-vs-runtime (provider-exchange-note.md L17-18, RULES.md L56-58).

## The 6 TRUE STOPS as the spine
- R3 (antifragility), R6 (gate ownership), R7 (demand), R8 (Owner terminus), R9 (plurality), R10 (foundherence).
- **R3, R6, R7, R8, R9, R10** are the 6 stops the doctrine rests on. R1, R2, R5, R11, R12 are operative derivatives (see TRUE_STOPS.md).

## The 3 debts as W03's first work
- **UW01 (LB04)**: write the positive form of "results are the prices" — or fully demote.
- **UW02 (LB07)**: name the external wall for the dead orchestrator — or fully demote.
- **UW03 (LB09)**: name the same-task operator — or fully demote.

## W03 task list
- For each UW, attempt the missing record-piece. If attempted and T-ANCHOR + T-NONCIRC
  pass, retire the UW. If attempted and a test fails, KILL the LB.
- For each of the 14 stable terms that passed the TRUE STOP test, no work needed.
- For each of the 6 PRIMITIVE-COSTUME terms, decide: keep as costume (with caveat) or retire.
- Re-test LB01..LB12 against the 6 TRUE STOPS — every load-bearing claim must reduce to
  one or more of R3/R6/R7/R8/R9/R10. If a claim reduces to *none* of these, it is costume.
- Update BECAUSE.md's "tests" column to include a `REDUCES-TO` row showing which of the 6 each LB terminates at.