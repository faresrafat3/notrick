# D3-Q07 — BECAUSE spine for 6 hole-claims (contrastive, non-circular)

Source: bore/HOLE.md only. For each claim: BECAUSE (contrastive — why this claim over its rival), INSTEAD-OF (what the claim displaces), IF-FALSE (what breaks if the claim is false). Circular check on every row: the BECAUSE may not quote the claim's own conclusion.

- BC1 | claim: "sampling alone only deters WHOLESALE fabrication ... never surgical single-line forgery" (L6)
  - BECAUSE: the per-draw hit odds (k/N, L3) scale with k — a one-line forgery (k=1,N=100) yields ≈0.951 evade (L4), so only large k makes the draw likely to land. (arithmetic support, not the claim itself)
  - INSTEAD-OF: "sampling tends to deter forgery in general" — the claim narrows deterrence to the k-large regime.
  - IF-FALSE: if sampling DID catch surgical single-line forgery, P(catch) would not collapse with k=1; the L4 number would not be "≈0.951 near-useless".
  - CIRCULAR? NO — reason cites k/N math, not the conclusion.

- BC2 | claim: "single sample is weak" (L1 heading)
  - BECAUSE: the only worked one-draw count gives P(evade)≈0.951 (L4) — one draw fails 19/20 times against a small lie.
  - INSTEAD-OF: "single draw is a meaningful gate" — the heading itself demotes the one-draw check.
  - IF-FALSE: the honest-bounds heading would be false; the 0.951 figure would be a typo-grade understatement.
  - CIRCULAR? NO — reason cites L4 numeric, distinct from the heading's verdict.

- BC3 | claim: "over 5 tasks ≈ 0.52" catch (L5, s=3)
  - BECAUSE: repetition of independent draws compounds the per-task miss: miss^5 with miss≈0.86 → evade ≈ 0.48 (arithmetic from L5's own numbers).
  - INSTEAD-OF: "escalation to s=3 alone is decisive" — the s=3 per-task catch (0.14) is still minor; only t=5 lifts it.
  - IF-FALSE: if 5 tasks did NOT≈0.52, the fragment's strongest number (the one it resells after confessing 0.951) is wrong — the whole credibility arc (TURN-1) collapses.
  - CIRCULAR? NO — reason counts repetition across tasks, not the claimed 0.52 itself.

- BC4 | claim: "Strength comes from the组合" (L6)
  - BECAUSE: the fragment offers no single mechanism that catches surgical forgery; it must point at the package (hash + danger escalation + repetition + honor-cost) as the aggregate answer to the boundary it just confessed (L6 both halves).
  - INSTEAD-OF: "strength comes from sampling" — the fragment itself ruled that out (L6 first half).
  - IF-FALSE: if the combination does NOT confer strength, the fragment's central sales move (Q05 official reading) has no support inside the 7 lines — which is already the finding of Q07-D2 (unpaid promise).
  - CIRCULAR? PARTIAL-RISK: "strength comes from the combination" BECAUSE "no single mechanism" is defensive, not circular; accepted as contrastive but flagged as the fragment's weakest spine link.

- BC5 | claim: "on any MISMATCH next task is s=full" (L7)
  - BECAUSE: a MISMATCH is the cheap-check's own failure signal (L7 lists it as the sharpest trigger), so escalating to full verification on that signal is the only escalation that re-covers ground the cheap check lost.
  - INSTEAD-OF: "escalate on schedule/time" — the fragment ties escalation to detected failure, not calendar (L7: "on any MISMATCH next task").
  - IF-FALSE: if s=full did not follow MISMATCH, the escalation law is decorative — the threat has no teeth, and "candidate" (L7) already concedes the schedule is provisional.
  - CIRCULAR? NO — reason cites the signal's role, not the schedule's conclusion.

- BC6 | claim: "Numbers rejectable by Owner" (L7)
  - BECAUSE: the fragment acknowledges its schedule and bounds are proposals, not law ("(candidate)" L7); deferring the numbers to the Owner is the fragment's only available legitimacy outside its own arithmetic.
  - INSTEAD-OF: "these numbers are settled" — the fragment explicitly refuses settlement.
  - IF-FALSE: if the numbers were NOT Owner-rejectable, the "candidate" hedge would be dishonest — the two hedges (Q02 repairs) stand or fall together.
  - CIRCULAR? NO — reason cites the candidate-hedge pairing, distinct from the deferral conclusion.

All 6 non-circular (BC4 flagged partial-risk). The spine's honest weight: BC1-BC3 rest on the fragment's own arithmetic; BC4 rests on the fragment's own boundary; BC5-BC6 rest on the fragment's own hedges.