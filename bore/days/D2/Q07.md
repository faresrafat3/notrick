# D2-Q07 — promise hunt: later/thus/therefore/shown not cashed in the hole

Source: bore/HOLE.md only. Every forward-reference/promise the fragment makes and fails to cash INSIDE lines 12-18.

| promise (HOLE L) | the word/claim | what it promises | cashed? |
|------------------|----------------|------------------|---------|
| L4 "P(evade) = (1−1/100)^5" | near-useless | "near-useless" for one task | PARTIAL-cashed: L6 explicitly names only-WHOLESALE-detection. The "near-useless" tag is honored by the negative claim in L6, but the RESCUE it implies ("strength comes from combination") is not cashed — L4-L7 never show the combination catching anything. |
| L4 "Strength comes from the组合: cheap hash + danger escalation + repetition + honor-cost" | comes from | the four listed elements confer strength | UNCASHED inside hole: no derivation, no arithmetic, no example where the combination catches what single sampling misses. The sentence is a thesis-the-promise. |
| L4 "cheap hash (binds the whole output against post-hoc edits)" | binds | hash prevents post-hoc edits from going undetected | UNCASHED inside hole: mechanism never shown, no edit-tamper example. (A2 in §4 — outside — would cash it, but that's the file, not the fragment.) |
| L6 "never surgical single-line forgery" | never | the guarantee is absolute | CASHED as a negative: it is the fragment's one unhedged assertion (Q02). But its CONSEQUENCE is not: if sampling never catches surgical forgery, what does the RECEIPT do about the surgical case? The hole leaves surgical threat uncovered — a promise (by omission) that something covers it. |
| L7 "Escalation schedule (candidate)" | schedule | escalation recovers missed fraud | UNCASHED: the effects of s=3 vs s=1 on catch are not computed here (Q06 noted). The promise "escalate and you catch more" has no number inside the hole. |
| L7 "Danger = budget-echo mismatch OR collision-count=3 OR every 3rd claim" | = | danger is exactly those three signals | UNCASHED as a definition: the three signals are named but no mapping is shown between a signal and a raise in detection. "every 3rd claim" has no justification (Q06). |
| L7 "Numbers rejectable by Owner." | rejectable | the numbers are provisional | CASHED as a hedge: it IS the reservation (Q02). But it promises the numbers are open; who evaluates the replacement? Also uncashed: the sentence implies an authority outside the fragment — an outside-lane, by definition uncashable here. |

Total: 7 promises; 1 cleanly cashed (L6 negative), 1 hedged-cash (L4 near-useless → L6), 1 authority-hedge (L7 Owner), 4 uncashed inside the hole (combination, hash-binding, escalation-recovery, danger-signal mapping). The fragment's own strongest word ("Strength comes from") is its biggest unpaid promise.