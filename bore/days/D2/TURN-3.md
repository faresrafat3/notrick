# D2-TURN-3 (Q15) — ASSAY: bland-rename the hole's local terms

Same hole, assay angle (kernel §5: does a reading survive substitution?). Rename sampler → inspector, lines → rows, hash → digest, forger → cheater, threat-family words → plain words, keeping every formula and structure intact. If the local spine dies under renaming, the fragment was branding; if it survives, it is load-bearing residue.

## Renamed rewrite (structure identical, terms bland)

> ## 2. Check math (candid bounds — one draw is weak vs sparse cheating)
> A cheater adds `k` bad rows to `N` total rows; the inspector draws one: `P(catch) = k/N`.
> `N=100, k=1, t=5 checks`: `P(evade) = (1−1/100)^5 ≈ 0.951` — near useless by itself. Said plainly so nobody overstates it.
> `N=100, k=5, s=3`: `P(catch) = 1 − C(95,3)/C(100,3) ≈ 0.14` per check; over 5 checks `≈ 0.52`.
> Conclusion recorded against my own proposal: a single draw only stops BIG cheating (`k` large) and sloppy lies — never precise single-line forgery. Strength comes from the mix: cheap digest + trigger escalation + repetition + visibility of checking cost.
> Escalation plan (tentative): base one draw; on a trigger three draws; on any FAIL next check is all rows. Trigger = echoed-budget mismatch OR tag-collision=3 OR every 3rd claim. Numbers rejectable by the boss.

## Verdict

SURVIVES. The renamed text carries every load-bearing finding from D2 identically: Q03's evasion ≈0.951; Q04's uniform/independent/no-replace assumptions; Q05's six silences (clean-draw meaning, order enforcement, trigger-deception, volume-blindness, s=full cost, visibility object); Q07's unpaid rescue ("Strength comes from the mix" — still uncashed); Q10's ornament kills. NOTHING the fragment taught us depended on sampler/hash/forger vocabulary. The spine is arithmetic + negative boundary + unpaid rescue — all rename-proof. The branding test FAILS to kill: this hole is residue, not costume (Q09-C06 remains the one promised-but-unpaid element, unchanged by renaming).

still_in_hole=YES — renaming performed on a copy; source pr-006 untouched.