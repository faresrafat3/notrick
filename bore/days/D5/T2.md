# D5-T2 — silence-drill: S2 (who chooses the index; how is order enforced) through the machine

Source: bore/HOLE.md only. D2-Q05-S2 asked: who chooses the sampler index AFTER the claim, and how is the order enforced? The hole is silent. Run it through the machine.

## What the machine has

- The machine (D4-Q05) has five fields (N, k, s, t, trigger) and four operations (draw, price, compound, escalate). Neither the fields nor the operations mention WHO picks which s lines, or WHEN the pick is fixed relative to the claim.
- The hole's own formulas depend on that missing order: P(catch)=k/N (L3) is only true if the s lines are chosen INDEPENDENTLY of which lines are faked. If the chooser knows the fakes (or the claimant can edit after seeing the pick), the formula's premise dies — P(catch) becomes whatever the chooser's information allows, up to 1 or down to 0.

## What the silence forces

1. The fragment's whole price sheet is conditional on an unstated protocol. L3-L5's numbers are not properties of sampling; they are properties of sampling-plus-blind-choice. The hole prices the draw but never purchases the blindness that makes the price honest.
2. Inside the machine there is no field for it: the schema has no "chooser" and no "order". So the hole's own machine cannot represent the condition its own formulas assume. The price sheet is written in a language whose grammar (per the fragment's own schema) cannot express its precondition.
3. This converts the escalation ladder's meaning: danger-signals trigger s=3/s=full (L7) — but if the chooser/order is unfixed, escalation changes the sample AFTER the claimant has already seen earlier samples. A claimant who survives one draw knows which lines were checked (they came back clean) and knows all fakes are in the remainder (D4-T1 localization) — so s=3 drawn by the same blind rule is fine, but ANY leak of chooser-identity or order lets the claimant place fakes in the ever-shrinking blind spot. Escalation without order-enforcement is a schedule for teaching the claimant where the blind spot is.
4. The fragment's honesty furniture does not cover this either: the boundary ("never surgical", L6) is ABOUT the claimant's placement skill; the order-silence is about the VERIFIER's procedure. The hole confesses the adversary's strength and stays mute about its own.

## Carry

New MUST_NOT candidate (extends MN1): "Never cite this fragment's probabilities without also citing a blindness/order protocol the fragment does not contain." The price sheet is honest only under conditions the 7 lines never state — the deepest defect found this week (worse than CE1: CE1 was a missing consequence; S2 is a missing PRECONDITION for all the numbers). still_in_hole=YES.