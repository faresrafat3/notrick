# D5-Q04 — workspace-infidelity check: did we invent readings the fragment does not bear?

Audit each load-bearing finding against HOLE raw text. Rule: derived-from-model = borne; interpretive gloss = flag; unsupported invention = revert.

| finding | where | does HOLE bear it? | verdict |
|---------|-------|--------------------|---------|
| Negative-clearance CORE: "given a miss, all k fakes sit in the unchecked N−s" (D4-T1) | derived from L3 (k fakes exist; s checked lines clean) | YES — placement-free certainty: if the draw missed, zero fakes were in the s checked, so all k are in N−s. Arithmetic of the model as written. | BORNE — keep |
| D4-T1 EXTENSION: "per-line fake probability in the unchecked region = k/(N−s), which exceeds k/N" | my derivation | PARTIAL — this assumes fakes are UNIFORMLY placed over the unchecked lines. HOLE never states fake placement (the forger is adversarial; placement is his choice, not a uniform random variable). The rate k/(N−s) is an expectation over placements, not a model fact. | PART-INVENTED — REVERT the per-line-rate sentence; keep the placement-free localization. |
| s=full arithmetic: "P(catch)=1 only at s=N; full costs N× the cheap draw (100× at the hole's N=100)" (D4-T2) | derived from L3/L4/L7 | YES for the math — s=N makes C(N−k,N)/C(N,N)=0; N=100 is the hole's own example size. BUT "full = check every line" is my READING of the word "full" (HOLE never defines it; D2-Q04 flagged it as a level, not an integer). | BORNE as conditional reading — keep, flagged: the 100× holds IF full=N; the hole itself never says so. |
| "s=full is a punishment trigger / nuclear option" (D4-T2) | interpretive | GLOSS — HOLE says "on any MISMATCH next task is s=full" (escalate on failure, literal). "Punishment/nuke" is my tone. | FLAG — demote to "escalates only after failure"; keep the economics. |
| Rescue-unpaid: "the combination is never cashed inside the 7 lines" (D2-Q07, D4-Q10) | textual absence | YES — L6 states "Strength comes from the组合" with no derivation/example/number anywhere in §2. Absence is a fact about the text. | BORNE — keep |
| Three live readings: limits-report / rescue-argument / disclosure-performance (D3-T3) | interpretive frames | Borne AS FRAMES: limits-report = L1/L4/L6-first; rescue-argument = L6-second/L7; disclosure-performance = the ORDERING (weak number first, repair clause, deferrals) which is literally in the text. None is claimed as THE meaning. | BORNE as live forks — keep, no revert |
| D4-Q08 KILL-B: "wholesale comfort uncalibrated" | critique of L6 | YES — HOLE says "(k large)" with no calibration; Run4 (k/N=10% → 81% evade at s=2,t=1) is legitimate arithmetic. The kill targets uncalibration, not the sentence itself. | BORNE — keep |

## Revert executed

REVERT-1 (D4-T1 per-line rate): the sentence "per-line fake probability in the unchecked region: k/(N−s). At s=1: k/(N−1)" assumes uniform fake placement, which HOLE does not state (the forger places adversarially; the model is silent on placement). The placement-free core survives: a miss proves all k fakes are in the N−s unchecked lines — that is certain and does not need placement assumptions. The "suspicion rises" consequence is DEMOTED from theorem to expectation-under-uniform-placement-assumption. Correction appended to days/D4/TURN-1.md.

No other reverts. The audit's residue: every kept finding is either (a) arithmetic forced by the model as written, (b) a fact of textual absence, or (c) a flagged interpretive frame held as a fork, never as the fragment's meaning. still_in_hole=YES.