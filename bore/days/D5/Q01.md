# D5-Q01 — embarrassing reduction: this hole is just X

The reduction to test: **the hole is just a worked example of acceptance-sampling / Bernoulli-trials arithmetic with a sales paragraph stapled on.** X = "detection-probability arithmetic for sampling inspectors, honest-bound form."

## Coverage test (does X cover most of HOLE.md?)

| HOLE content | covered by X? |
|--------------|---------------|
| L3 "Forger fakes k of N lines, verifier samples s=1: P(catch)=k/N" | YES — acceptance-sampling single-draw formula |
| L4 "P(evade)=(1-1/100)^5 ≈ 0.951" | YES — Bernoulli evasion across t trials |
| L5 "P(catch)=1-C(95,3)/C(100,3) ≈ 0.14 ... 0.52" | YES — hypergeometric sampling without replacement (textbook) |
| L6-first-half "never surgical single-line forgery" | YES — the classic small-lot escape from sampling (sample-size effect, textbook) |
| L6-second-half "Strength comes from the组合" | NO — this is the sales paragraph; X covers it only as "a claim appended to the arithmetic" |
| L7 escalation schedule + danger signals + Owner hedge | NO-as-math: X covers "escalate sample size on signal" (textbook adaptive sampling) but the danger list (budget-echo, collision-count, every-3rd) and the Owner deferral are non-sampling governance |

Coverage: of the fragment's load-bearing content (L3, L4, L5, L6-first-half), X covers 4/4 = 100%. Of the whole 7 lines including the sales + governance tail, X covers ~70% (the arithmetic + escalation) — the uncovered 30% is the combination-promise, the danger-signal borrowings, and the Owner hedge, all of which D2-D4 already classified as costume/stipulated (D4-Q09 machine residue excludes them).

## Verdict: HIT

The hole is just acceptance-sampling arithmetic with an honesty label and an unpaid rescue paragraph. The embarrassing part is not that it is weak — it is that EVERY formula, every boundary, every insight the fragment produces is textbook: k/N single-draw, hypergeometric without-replacement, t-trial evasion compounding, sample-size small-lot escape. A QA engineer could have written L3-L6-first-half from any introductory statistics text — the fragment's own D4-Q09 bland-rename proved the machine is commodity. X covers the load-bearing 100% and the local "theory" D1-D4 built is a theory of textbook arithmetic dressed as proposal-anchored discovery.

## What survives the reduction (to carry)

The reduction kills the fragment's PRETENSE (that it is proposal-specific or novel math), but the drill's own findings survive as LOCAL facts about THIS text: the negative-clearance theorem (D4-T1), the s=full punishment economics (D4-T2), the three live readings (D3-T3), the unpaid-promise map (D4-Q10). Those are claims about this fragment's assembly, not about sampling math — X explains the arithmetic; it does not explain why the fragment SCHEDULES s=full only as punishment, or localizes fraud in the blind spot as conclusion rather than design. That residue is genuinely of-the-hole. still_in_hole=YES.