# D4-Q09 — substitution: machine-level bland-rename (D4 machine)

D2-TURN-3 renamed SENTENCES and the spine survived. This quantum renames the MACHINE (D4-Q05: fields F1-F5, operations O1-O4, the negative bound) to see what survives as residue. If the machine dies under renaming, the hole's load was branding.

## Machine with bland names

- F1 N  →  total items in a batch (was: total checkable lines)
- F2 k  →  bad items the adversary planted (was: faked lines)
- F3 s  →  items inspected this round (was: draw size)
- F4 t  →  rounds so far (was: tasks)
- F5 trigger  →  the alert state (was: danger/MISMATCH)

- O1 sample-at-rate  →  inspect s items (was: draw)
- O2 price-an-event  →  probability of catching a bad item (was: P(catch))
- O3 compound-across-tasks  →  raise per-round miss to the t-th power (was: (1−p)^t)
- O4 escalate-on-signal  →  bind next inspection count to alert (was: next-s)

- negative bound  →  small k is not deterred: batch of 100, one bad item, five rounds of one-item inspection → ~95% escape (was: L4 0.951)

## Does the spine die?

No. The renamed machine is still fully runnable: schema (batch, bad, inspected, rounds, alert) → price + next-inspection-count, with the same four operations, same formulas (k/N, 1−C(N−k,s)/C(N,s), ^t), same unpriced "inspect everything" level (s=full), same negative-boundary sentence. Every D4 finding transfers verbatim: the no-slot-for-rescue claim (Q04), EX2's line-level/output-level jump (Q06), the generator (Q07 — it reads the bland machine identically), the four runs (Q08 — RUN3 = 10% of items inspected, one planted bad item, 73% escape, still true).

## Residue vs branding

- RESIDUE (survives renaming): a batch-inspection machine that prices single-round and compounded detection, triggers escalation on alert, and concedes in its only unhedged sentence that small planted defects escape inspection. This is ordinary acceptance-sampling arithmetic (the hole's "novelty" claim, D3-Q10, is wholly costume).
- BRANDING (dies under renaming): nothing machine-level dies. The words that DID carry flavor — sampler, forger, receipt, hash, honor-cost, combination, WHOLESALE/surgical/lazy-lies, budget-echo, collision-count — are either absent from the machine (no slot) or already proven decorative (D2-Q06, D4-Q03). The machine's residue is so bland it could have been written as an inspection-quality note in any QA manual. That is the substitution verdict: the hole's MACHINE is commodity; its distinctiveness was entirely in the costume layer.

## Keep-only formula

The residue any future citation may use: P(catch)=k/N (single), 1−C(N−k,s)/C(N,s) (s draws), (1−P(catch))^t (t rounds), escalate-on-alert, negative bound for small k. Everything else the 7 lines add is either assertion (combination), hedge (candidate/Owner), or pointer (budget-echo/collision-count). still_in_hole=YES.