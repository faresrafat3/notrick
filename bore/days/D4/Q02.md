# D4-Q02 — illegal moves the hole forbids or should forbid (X1-X6, quoted)

Each X: a move the fragment's own text rules out (forbids already) or should rule out (forbids by implication of its numbers). Quotes from HOLE.md.

- X1 | CLAIM-A-CLEAN-DRAW-PROVES-THE-REST | move: after a draw misses the faked lines, assert the other N−s lines are honest. Forbidding quote: "P(catch) = k/N" (L3) + L5's C(95,3)/C(100,3) — the model prices a hit on faked lines only; a miss says nothing about the N−s unchecked lines (D2-Q05 S1). The move silently upgrades "the draw did not land" into "there is nothing to find". The hole makes this illegal by never claiming it; any later doctrine citing this fragment must not either.
- X2 | EQUATE s=3 WITH PROGRESS-BY-ESCALATION | move: say "raising the draw to 3 catches 3x more, so escalation is the recovery." Forbidding quote: L5 "P(catch) = 1 - C(95,3)/C(100,3) ≈ 0.14 per task" vs L3 "P(catch)=k/N" (0.05 at k=5) — a 3x lift from a 5% base is still an 86% miss per task; only "over 5 tasks ≈ 0.52" (L5) crosses half, and that is repetition t, not s. The hole's numbers make "escalation saves" an overreach the fragment itself prices as still-losing.
- X3 | PROMOTE-THE-COMBINATION-TO-RESULT | move: treat "Strength comes from the组合" (L6) as a demonstrated finding. Forbidding quote: L6's own structure — the sentence names four items (hash, danger escalation, repetition, honor-cost) with no derivation inside the 7 lines (D2-Q07 unpaid); and L7 marks the surrounding schedule "(candidate)". The hole should forbid citing its combination as proven; it does so by never cashing it.
- X4 | USE-s=full-AS-A-NUMBER | move: plug s=full into P(catch)=k/N or price its detection. Forbidding quote: L3 "s=1" and L7 "s=full" — "full" is a level, not an integer (D2-Q04); the formula only evaluates integer sample sizes. The hole should forbid arithmetic on s=full; it does by leaving it defined only by contrast with s=1/s=3.
- X5 | CLAIM-THE-HASH-VERIFIES-TRUTH | move: "the hash binds the output, therefore the output is checked for truth." Forbidding quote: L6 "cheap hash (binds the whole output against post-hoc edits)" — binding against edit ≠ verifying truth; the parenthetical itself limits the hash to edit-detection (D3 GRAVEYARD G2-adjacent). Any use of the fragment to license truth-by-hash is a move its own sentence forbids.
- X6 | DELEGATE-THE-LAW-TO-THE-OWNER-AS-CHEAP-ESCAPE | move: use "Numbers rejectable by Owner" (L7) to stop all further scrutiny ("Owner can fix it, so the numbers stand"). Forbidding quote: L7 "(candidate)" + "Numbers rejectable by Owner" — the Owner-deferral is a hedge on a candidate schedule, not a permission for the arithmetic to avoid scrutiny: the fragment itself priced k/N, C(95,3)/C(100,3), and the evasion inside its own lines (L3-L5) before deferring anything. The hole should forbid citing Owner-deferral as a validity end-run; its own unpriced-debt structure (D2-TURN-2) implies it.

All X1-X6 are grounded in the fragment's own quotes; none imports foreign doctrine.