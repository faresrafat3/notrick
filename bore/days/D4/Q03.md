# D4-Q03 — relations/operations the hole actually performs, not names

Source: bore/HOLE.md only. Difference from Q01 (primitives = objects) and Q02 (illegal moves): here, WHAT THE MACHINE DOES — the operations its text actually executes with its numbers. Each operation must be visible as an action in the 7 lines, not a named concept.

## O1 | SAMPLE-AT-RATE (perform the draw at size s)
Text performs: L3 "verifier samples s=1"; L5 "s=3"; L7 "base s=1 ... s=3 ... s=full" — the act of selecting s lines. The machine actually draws; that is O1. Characterized by size s and population N.

## O2 | PRICE-AN-EVENT (compute a probability from k, N, s, t)
Text performs: L3 "P(catch) = k/N"; L4 "(1-1/100)^5 ≈ 0.951"; L5 "1 - C(95,3)/C(100,3) ≈ 0.14 ... ≈ 0.52" — the hole's signature operation: turn (k, N, s, t) into a number. It never computes anything else.

## O3 | COMPOUND-ACROSS-TASKS (raise miss to the t-th power)
Text performs: L4 "(1-1/100)^5" and L5 "over 5 tasks ≈ 0.52" — the only non-linear operation in the hole: repetition as exponentiation of evasion. The machine's sole compounding.

## O4 | ESCALATE-ON-SIGNAL (bind the next draw size to a trigger)
Text performs: L7 "on danger-signal s=3; on any MISMATCH next task is s=full" — the control-flow operation: observed signal → next s. The machine's only conditional, and it is the fragment's claim about its own behavior.

## O5 | DISCLOSE-THE-ODDS (publish the weak number before the strong one)
Text performs: L4 publishes 0.951 FIRST, "near-useless alone. Stated plainly so nobody oversells it", then L5 publishes 0.52. This ordering is an operation of the fragment (TURN-1/TURN-3: the disclosure performance). Not a check-machinery operation but an actual textual behavior.

## What the hole does NOT perform (named but unexecuted)
- It does not perform hashing (L6 names the hash; no hash operation shown).
- It does not perform "honor-cost" accounting (L6 name only).
- It does not perform danger-DETECTION: it names signals (L7 budget-echo, collision-count, every-3rd) but never runs detection — the signals are inputs quoted from outside (SITE.md quote-only).
- It does not decide anything: no verdict operation, no approve/reject — the fragment's last line delegates "Numbers rejectable by Owner" (L7).

## Machine summary
The hole's operating core is O1→O2→O3 (draw → price → compound) with O4 as its claimed behavior and O5 as its persuasive ordering. Anything about hash/honor-cost/danger as OPERATIONS is a name without a body (matches D2-Q06 excess).