# D4-Q01 — primitives forced by the hole (<=5; each must occur in HOLE.md)

Rule: a primitive is illegal unless its token appears in HOLE.md. Dry, no gloss. From the 7 lines, the machine needs exactly these (each occurrence quoted):

- P1 | DRAW (s) | "verifier samples s=1" (L3); "base s=1; on danger-signal s=3; ... s=full" (L7) | the count of checked lines per task; the machine's only action primitive — a draw against N lines, valued at s, charged by the odds table.
- P2 | LINES (N) | "`k` of `N` lines" (L3); "N=100" (L4, L5) | the total checkable population; the denom of every probability in the hole.
- P3 | FAKED (k) | "fakes `k` of `N` lines" (L3); "k=1" (L4); "k large" (L6) | the count of fabricated lines the opponent controls; the hole's only opponent parameter.
- P4 | SCHEME (sampler) | "Sampler math" (L1); "sampling alone" (L6) | the aggregate decision rule (which draw sizes, under which triggers) — the thing being priced.
- P5 | ESCALATE-ON (danger/MISMATCH) | "danger-signal s=3" (L7); "on any MISMATCH next task is s=full" (L7) | the trigger relation: observed signal → next draw size; the machine's only control flow.

Justification: P1-P3 are the arithmetic's three variables (all sized by the hole's own numbers 100/1/5/3); P4 is the subject of every sentence; P5 is the only conditional the fragment states. Everything else in the hole (hash, honor-cost, budget-echo, collision-count, combination, tasks-repetition t, WHOLESALE/surgical/lazy-lies) is either a borrowed identifier (SITE.md quote-only) or a named-but-unoperated object (D2-Q06) — not a primitive the machine must carry.

Explicitly NOT primitives (occur but not forced): t/repetition (named P2's t=5 but the machine reduces it to repeating P1); the four strength names (honor-cost etc. — no operation shown); Owner (a hedge authority, no machine role); the enemy-class trio (labels on k, redundant with P3's size).