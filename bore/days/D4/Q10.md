# D4-Q10 — map survivors to unpaid promises in the hole

Unpaid promise list (D2-Q07, from HOLE): U1 combination-gives-strength · U2 hash-binds-output · U3 escalation-recovers · U4 danger-signal mapping · U5 Owner-rejectable numbers (hedge-cash) · U6 "never surgical" consequence (what covers the surgical case).

Day-4 survivors: Run1 (N=500,k=2,s=1,t=5 → 0.98 evade), Run3 (N=50,k=1,s=5,t=3 → 0.73 evade), machine residue (k/N, 1−C(N−k,s)/C(N,s), ^t, escalate-on-alert, small-k bound).

## Map (survivor → promise)

| survivor | promise it touches | paid? |
|----------|--------------------|-------|
| Run1 (0.98 evade) | U6 (what covers surgical) | PARTIAL-NEGATIVE: makes the surgical-case gap LOUDER — at k=2,N=500 nothing in the hole covers it. Does not pay; sharpens the debt. |
| Run3 (0.73 evade at s=5/N=50) | U3 (escalation recovers) | KILLS: 10%-coverage draw still evades 73% — escalation-only recovery is dead on the machine's own runs (joins Q08 KILL-A). |
| Residue formula 1−C(N−k,s)/C(N,s) | U2 (hash binds) | TOUCHES-NOTHING: the residue formula is line-level sampling; hash is an output-level external binder (EX2 jump). The machine has no slot; U2 stays unpaid (outside fragment by design). |
| Residue escalate-on-alert | U4 (danger-signal mapping) | TOUCHES-NOTHING: the machine says alert→next-s but carries no signal-detection (signals are quote-only pointers, SITE.md). U4 stays unpaid. |
| Residue negative bound (small-k) | U5 (Owner-rejectable) | NONE: the bound is arithmetic, the Owner hedge is governance; disjoint. U5 stays a hedge, not a promise. |
| Run1+Run3 combined | U1 (combination-gives-strength) | STILL UNPAID: no run of the machine produces a number where the FOUR-name combination catches what single sampling misses; the names have no schema slot (Q04). U1 remains the hole's biggest unpaid promise. |

## Verdict: none of the unpaid promises got paid by Day-4 survivors

Per the instruction ("If none paid, two targeted runs"), the two targeted runs were already executed as Kill-Evidence in Q08 (Run2 escalation-inert, Run4 wholesale-uncalibrated). Neither paid a promise; both killed one. So the mapping state is: 0 promises paid, 2 promises killed (U3 via Run2/Q08-KILL-A and via Run3 here; U6 sharpened, not paid), 3 still unpaid (U1, U2, U4), 1 hedge-only (U5).

## What this means for the cylinder

The hole's honest cargo is entirely negative: a pricing bound + a small-k boundary + a kill of escalation-recovery. Its positive cargo (combination, hash-binding, danger-mapping) is not only unpaid — it is un-payable by the machine the fragment itself built (no schema slot, EX2 jump). Any future citation that uses this fragment to justify the receipt scheme's STRENGTH is using an unpaid promise; the only citable residue is the machine + its negative boundary (D4-Q09). CORE.md will carry this as a hard constraint. still_in_hole=YES.