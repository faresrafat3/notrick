# D7-T1 — post-freeze anchor binding (kernel-permitted: "later wakes may bind anchors")

The user's standing order ("keep going") is executed within the freeze (days/D7/Q09.md): the one legal move that adds substance without a module or a new site is anchor binding — attaching exact source locators to CORE claims whose anchors were ledger-internal labels.

## What was bound

CORE §10 previously anchored its five quotes with HOLE-internal labels only (L3..L7). Bound to exact source line numbers in proposals/pr-006-receipt-attacks-and-math.md:

| HOLE label | content | source line |
|------------|---------|-------------|
| L3 | `P(catch) = k/N` | pr-006:14 |
| L4 | `≈0.951 — near-useless alone. Stated plainly so nobody oversells it.` | pr-006:15 |
| L5 | `≈0.14 ... ≈0.52` | pr-006:16 |
| L6 | `never surgical single-line forgery` + `Strength comes from the组合` | pr-006:17 |
| L7 | `(candidate)` + `Numbers rejectable by Owner` | pr-006:18 |
| heading | `## 2. Sampler math (honest bounds...)` | pr-006:12 |

Blank source lines 13/19 confirmed OUTSIDE the hole — the hole is exactly 12-18 minus blanks, i.e. 7 lines, 5 content sentences + heading, matching bore/SITE.md.

## Verification

- Mapping checked against the source with grep -n (this wake): each quoted string sits on the stated line — no claim moved, no content changed.
- HOLE.md re-diffed byte-exact against source lines 12-18 before binding (this wake): HOLE_STILL_EXACT.
- Version stamp bumped v1.1 → v1.2 with the binding note; §10 and §14 are the only lines touched (additive precision, per freeze MAY-list).
- Labeling slip self-caught and fixed: first wrote "D8-T1" (no day 8 exists) → corrected to D7-T1 before logging.

## FAIL-row check (freeze MAY-list item 2)

LOG audit: 100 lines, 100 PASS, 0 FAIL — no FAIL rows to repair. NC1-NC4: no new evidence exists on disk (L6: none invented); all four still standing with their kill-conditions.

still_in_hole=YES.