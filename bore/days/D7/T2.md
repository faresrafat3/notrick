# D7-T2 — freeze integrity check (post-binding)

Machine-verified this wake (python, output preserved in session log):

- Section order: CORE.md contains exactly numbered sections 1-14, in order — the kernel's DAY 7 Q02 order intact after the D7-T1 binding edit.
- Section keywords (Site locator → doctrine → Practiced → splits → primitives → Because-spine → Negative → Break cases → Contact prohibitions → Evidence quotes → FORBIDDEN → Exhaustion → Standing orders → Version stamp): all present, positions strictly monotonic.
- Quote integrity: all four load-bearing strings quoted in §10 (`P(catch) = k/N`, `near-useless alone`, `never surgical single-line forgery`, `Numbers rejectable by Owner`) remain verbatim inside HOLE.md — binding changed locators, not content.
- Object list: the 14 top-level .md files on disk exactly match the frozen object list from days/D7/Q09.md — no module added, none removed, no stray file.
- HOLE.md byte-exact vs source (re-verified at wake start, D7-T1).

Result: the freeze holds after densification. v1.2 = v1.1 + additive anchors only. No FAIL rows exist (LOG: 102 lines, all PASS, zero still_in_hole=NO). NC1-NC4 unchanged — no new evidence on disk, none invented (L6).

Remaining legal moves after this: none that produce non-duplicate product. Anchor binding done (T1); FAIL rows: none; re-entry protocol: already 8/8 at D6-Q05, re-running on an unchanged fragment would be duplicate product (L16); HOLE re-verify: done twice this wake. Per the freeze and L16, the correct next move is STOP, not manufactured work. still_in_hole=YES.