# PROPOSAL — PR-004

> date: 2026-09-06 · author: guest (opencode) · status: proposed-in-file · touches: NONE (ruling request only — no doctrine text proposed)

## The gap + intersecting entries

`H4` needs 3 `RJ`s with binding conditions (`hypotheses-ledger.md:27-29`), but the council is `refining` with an empty Owner field (`charters/ch-002-council.md:2,46`; `ONBOARDING.md:9-10`). The sole precedent is `research/rj-001-premises-ruling.md:1` — `paper ruling by Cline as presiding`. First-task `RJ`s will therefore be paper by necessity, and the `3-RJs-with-zero-conditions → theater` rule would judge paper theater, not the layer.

Intersects: `RULES.md:33-35 R8` (Owner terminus) · `VERSION.md:10` (signature gates `v0.0001`) · `ZERO-READINESS.md:14` (signature is the remaining A-line).

## Proposed ruling (two options, Owner picks one — I fill nothing)

- Option A: pre-signature `RJ`s are `paper-only` and excluded from the `H4` quorum; counting starts at the first post-`v0.0001` `RJ`.
- Option B: paper `RJ`s count, but the theater verdict requires 3 post-signature `RJs` to confirm.

Rejected alternative recorded: counting paper as binding now — pollutes the measurement with enthusiasm rather than law (violates `R8` spirit).

## Forcing result

Charter status lines + `RJ-001` header — no run needed. `PENDING` until first council round is demanded.

## Falsifier

If the Owner signs `CH-002` before any `RJ` is needed, this question evaporates — close with the signature date cited.

## Owner action needed

Written ruling: A or B (one letter). Signature field itself untouched by me.
