# PROPOSAL — P-020
> the ONLY thing a guest may create. Filed in proposals/. Never edited after filing
> (corrections = a new proposal citing the old one). Owner reads, rules, and only an
> approved-docs moment moves its content into doctrine.

## Meta
- **ID:** P-020 · **filed:** 2026-09-08 · **by:** executor, SERIES 2 R7 repair (glm-5.3-flash session)
- **boot-gate quote (must match the agent's first reply):** "# VERSION — v0.00001" · rights: operator-named executor for ROUNDS 6-7 repair only — not Owner, not a charter, not a guest
- **type:** rule proposal (proposal expiry — the 2-session Omega rule, made executable)
- **supersedes:** the proposals row of proposals/PROPOSAL-T1-HEIR-CLAUSE.md §2 — differences stated below

## The proposal (one sentence)
A proposal the Owner has not ruled on for two consecutive Owner sessions auto-retires via Omega-5 — verdict: retire (citing THIS clause as the firing obituary), lesson: one line extracted to the next filing, burial: a `HELD-EXPIRED · <date>` line appended by the next filing executor, heir: a NEW proposal citing the expired one (the no-edit law makes refile-with-citation the only continuation path) — and the Owner may re-arm a proposal any time before expiry by typing HELD + date in its ruling field.

## Where it lives (if adopted)
An `EXPIRY:` field added to templates/PROPOSAL.md's Meta block (doctrine — Owner's approved-docs moment) + the retirement line format in this file's evidence block.

## Evidence (R10 — every claim crosses an existing entry)
- The gap: proposals accumulate with no expiry (pr-001..pr-013 + PROPOSAL-T1/T5 all unruled as of 2026-09-08; T1 notes "a proposal unanswered in 2 Owner sessions auto-becomes HELD — expired — not implemented").
- The template's no-edit law (templates/PROPOSAL.md:2-3: "Never edited after filing — corrections = a new proposal citing the old one") already forces the refile-cite path this proposal names as heir — the expiry rule adds only the clock, not a new mechanism.
- Omega-5's five duties apply at ANY ending (research/arc-protocol.md:30-38) — an expired proposal is an ending, and no proposal in the repo has ever received the five duties; the expiry rule is what makes them fire.
- **Differences from T1's proposals row:** T1 adds BOTH a `HEIR:` header field (every proposal points at its successor at birth) and the 2-session auto-`HELD — expired` line. Two corrections: (1) a mandatory HEIR header is often unknowable at filing (which proposal supersedes this one is usually not yet known) — arc-protocol.md:36 allows "heir named, OR orphan declared"; the expiry-time refile IS the named heir when a successor exists, and the orphan declaration when none does — one field, two legal states, no forced fiction; (2) T1 says the expired proposal "auto-becomes HELD" — but HELD is a RULING word (templates/PROPOSAL.md:18) and an auto-ruling is a claimant-owned verdict; the retirement line must read `HELD-EXPIRED` (a distinct, clock-caused state) so no one can confuse a stale proposal with one the Owner actually held.
- Crosses the register paradox: this proposal, like every register artifact, cannot expire itself into silence — the burial line is appended by the NEXT FILING executor (a separate agent), keeping the measurer/disinterest leg (arc-protocol.md:26) intact.

## Owner ruling
- {APPROVED into doctrine via {date} approved-docs moment | REJECTED, reason | HELD}
- *(empty-by-right — the guest NEVER fills this)*
>
> obituary: this proposal dies the moment an Owner-ratified expiry field exists in the
> template (then this file is the ancestor, cited), or the moment any proposal is retired
> WITHOUT the Omega-5 five duties — then the rule was adopted but the death law it exists
> to serve was not, and the gap is the finding.