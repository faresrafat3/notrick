# PROPOSAL — PR-003

> date: 2026-09-06 · author: guest (opencode) · status: proposed-in-file · touches: research/decision-review.md (measurement annex, no edits made)

## The gap + intersecting entries

Five falsifiers lack a named measurer, so they stand philosophically and stall on zero day: `D-01` quality (`decision-review.md:10-11`), `D-02` win/failure counts (`:18-19`), `D-03` "systematically" (`:25-26`), `D-05` "days" (`:39-40`), `H3` "inspect manually" (`hypotheses-ledger.md:22-23`).

Intersects: `templates/RJ.md:9-16` four seats · `templates/DS.md:24-29` result record + K-line · `charters/ch-002-council.md:36-40` seats · `mics/mic-31-axis-tagger.md:16-17` collision counts.

## Proposed text (mechanisms only — every Owner value left blank)

1. `D-01 quality`: grader = Seat IV (Human), scale proposal `0-2 distance-from-intent` written into result record; quorum stays `≥3 tasks`. Scale values are mechanism, the passing line is NOT set here.
2. `D-02 win/failure`: win = task with zero mid-run `NO NICHE` surprise (borrows `ch-004` metric `ch-004-chief-of-staff.md:22-23`); failure = `NO NICHE storm` or stalled handoff quoted from `DS`. Counter = `MIC-31` tags + one `handoff:` line. Thresholds unset.
3. `D-03 systematically`: candidate = two consecutive fate decisions contradicted by later outcomes. Number 2 is rejectable — Owner may set 3.
4. `D-05 days`: clock = `CONTEXT.md` decision-log tail dates (existing record); day-count value left `___ empty` — Owner fills at zero or never.
5. `H3 inspector`: candidate = Seat II (Evidence) via mini-`RJ` after 5 runs. Assignment is proposal; mandate needs ratified council.

## Forcing result

Texts themselves contain the vague terms — no execution needed. Landing criterion: `DS-001` forces each term live at first use.

## Falsifier

If `DS-001` runs and each term is measured without dispute under current wording, this annex was unnecessary — kill with the `DS-001` ID cited.

## Owner action needed

`approve-docs` per mechanism independently (approve/reject line by line). Nothing here fills threshold, breakthrough, or signature fields.
