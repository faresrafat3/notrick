# PROPOSAL — PR-002

> date: 2026-09-06 · author: guest (opencode) · status: proposed-in-file · touches: research/hypotheses-ledger.md + research/decision-review.md (headers only, no edits made)

## The gap + intersecting entries

`H1..H5` and `D-01..D07` mature on different horizons (1 / 3 / 5+ tasks) but both ledgers present them flat — a task-1 judgment on a 5-task hypothesis would be a false failure, violating `RULES.md R6` spirit (verdict must be owned fairly) and `RJ-001` condition 1 (first task is ONE falsifiable experiment, not five).

Intersects: `research/hypotheses-ledger.md:11,17,22-23,29,35-36` (quorums 2-of-3, 3 RJs, 5 runs, 3 hits) · `research/decision-review.md:18-19,25-26` (5-task windows) · `ZERO-READINESS.md:33-37` dependency chain.

## Proposed text (header insertion candidate, doctrine untouched)

```
## Quorum schedule (read before judging — no verdict before quorum)
- Tier-0, first full task: H2 verdict allowed · D-06 first count · H4 accumulation only
- Tier-1, three tasks/RJs: H1 (2-of-3) · H4 (3 RJs) · D-01 review · D-07 reforge count
- Tier-2, five tasks/runs+: D-02 (5 tasks) · H3 (5 runs) · D-03 chain · H5 (3 K-line hits)
- Rule: before quorum, the only lawful status is `collecting` — never stands/dead/theater.
```

## Forcing result

Ledger texts themselves state the quorums; no run needed. `PENDING` operationalization — landing criterion: first `DS-001` that must cite a verdict rule hits the ambiguity live.

## Falsifier

If the first real task completes and every `D/H` is judged without dispute under the flat reading, this scheduling is decorative — kill it and record the task ID that disproved the need.

## Owner action needed

`approve-docs`: allow the header into both ledgers (insertion only, zero wording changes to existing `D/H` lines). No Owner-by-right value filled.
