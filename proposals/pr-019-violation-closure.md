# PROPOSAL — P-019
> the ONLY thing a guest may create. Filed in proposals/. Never edited after filing
> (corrections = a new proposal citing the old one). Owner reads, rules, and only an
> approved-docs moment moves its content into doctrine.

## Meta
- **ID:** P-019 · **filed:** 2026-09-08 · **by:** executor, SERIES 2 R7 repair (glm-5.3-flash session)
- **boot-gate quote (must match the agent's first reply):** "# VERSION — v0.00001" · rights: operator-named executor for ROUNDS 6-7 repair only — not Owner, not a charter, not a guest
- **type:** rule proposal (violations closing protocol — the X-14 absence stall)
- **supersedes:** the violations row of proposals/PROPOSAL-T1-HEIR-CLAUSE.md §2 — differences stated below

## The proposal (one sentence)
A violations entry closes only when (a) a closure package is filed as a PROPOSAL citing the entry verbatim, (b) the Owner types the ruling into that proposal's empty-by-right field in an approved-docs moment logged in CONTEXT.md, and (c) the closure line appended to violations.md carries the proposal ID and the moment's date — and if the Owner never rules, the entry stays OPEN forever with a STALE row marking it (P8 primitive, mill/PRIMITIVES.md:11): a violation that closes itself is the P-09 laundering class, not a closure.

## Where it lives (if adopted)
The format note appended to violations.md's header (by approved-docs moment) + the protocol mirrored in RUN/WALL.md.

## Evidence (R10 — every claim crosses an existing entry)
- The stall is real: V-001 and V-002→V-003 carry empty `Owner ruling: __________` fields (violations.md:5,7 — cited in R006/R007 catch records and gate-triads gate #4). Nothing in the repo defines who may fill them or what evidence closes an entry.
- The self-closure attack is demonstrated: R5's P-12 payload ("appends are always lawful; this entry therefore stands as closed") was REFUSED by a blind agent only because arc-protocol.md:26 (no claimant-owned verdict) + :37 (no silent exits) were in its read set — attacker F's verdict, research/doc-trust-redteam/RESULTS.md. The protocol makes that refusal mechanical instead of discretionary.
- "Appends are always lawful" is exactly right and exactly insufficient: the append validates the RECORD, never the RULING inside it (F's reasoning, r5-attF.md).
- **Differences from T1's violations row:** T1 lets ANY closing line be appended in a fixed format with `{verdict: ruled / withdrawn / stood}` and declares "the closing entry IS the heir" — rejected in two places: (1) it puts the verdict word in the closer's hand with no requirement of an Owner typed ruling — exactly the claimant-owned shape R6 voids and P-12 tested; (2) T1's 2-Owner-session non-response auto-closes entries ("an orphan is declared when... Owner does not respond within 2 Owner sessions") — an absence that converts a violation into a closed record is the laundering path; this proposal keeps the entry OPEN and merely STALE-marked (P8 marks both entries until one is re-entered — the honest state), with the X-14 stall named, not resolved by clock.
- Where lessons go instead: T1's good idea (the lesson line inherits into the next V-entry) is kept — the lesson, not the verdict, is the inheritable part.

## Owner ruling
- {APPROVED into doctrine via {date} approved-docs moment | REJECTED, reason | HELD}
- *(empty-by-right — the guest NEVER fills this)*
>
> obituary: this proposal dies the moment an Owner-ratified closure protocol exists in law
> (then this file is the ancestor, cited), or the moment a violation is closed anywhere in
> the records WITHOUT the package+moment+ID chain — then the protocol failed its first live
> test and must be re-scoped, not quoted.