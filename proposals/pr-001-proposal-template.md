# PROPOSAL — PR-001

> date: 2026-09-06 · author: guest (opencode, full-permissions delegation in chat) · status: proposed-in-file · touches: templates/PROPOSAL.md (missing) + proposals/ (new)

## The gap (one sentence + intersecting entries per R10)

`AGENTS.md` BOOT-GATE/GUEST-MODE orders proposals via `templates/PROPOSAL.md` into `proposals/`, but a verified `find` this session shows 24 files with neither path present — a law pointing at void, breaking `RULES.md:43-48 R10` (a claim intersecting nothing does not enter the grid).

Intersects: `AGENTS.md` GUEST MODE · `RULES.md` R10 · `CONTEXT.md:132` pace ruling (chat-only unless approved) · `ONBOARDING.md:17-20` propose-right.

## Proposed text (verbatim candidate for templates/PROPOSAL.md)

```
# PROPOSAL — PR-{NNN}
> date · author · status: proposed-in-chat | proposed-in-file | approved | rejected | superseded · touches: {file}
## The gap (one sentence + intersecting entries per R10)
## Proposed text (verbatim replacement or insertion)
## Forcing result (which record demands this, or PENDING + landing criterion)
## Falsifier (what future record kills this proposal)
## Owner action needed (approve-docs / signature / value — never pre-filled)
```

## Forcing result

Session `find` listing 24 files, zero hits for `PROPOSAL` or `proposals/` or `violations.md` — live-counted 2026-09-06 in `notrick/`. No doctrine file edited to learn this.

## Falsifier

If `templates/PROPOSAL.md` or an equivalent is ratified with a different form, this PR is superseded on sight — record the superseding file + date, kill this text.

## Owner action needed

`approve-docs`: ratify a proposal form (this text or yours) into `templates/` — form only. No values, no signatures, no threshold touched. Until then this file lives in non-doctrine `proposals/` and binds nothing.
