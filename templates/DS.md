# DESIGN PAGE — DS-{NNN}
> the Chief of Staff's ONLY product · {date}
> status: {draft | council-reviewed | active | done}
> council ruling: {RJ-xxx | not required — below threshold}

## End state (intent, not tasks)
{ONE sentence — the world after success}

## Schwerpunkt (the point whose fall opens everything)
{one point}

## Budget (hard)
- tokens: {N} · time: {N} · attempts: {N}
- global prohibitions: {…}
- owner threshold override: {…} ← the only empty-by-right field in the system

## Burrow map (one niche per line; a line without a niche = decomposer calls the Maker)
- {task line} → {CH-xxx | NO NICHE — call Maker}
  - in: {doc} · out: {doc}

## Verification gate (what the Verifier runs — exactly)
- {runnable command / test / criterion — never narrative}

## Result record (filled AFTER execution — the swarm's only voice)
- what actually happened: {…}
- documented result: {number / link}
- K-line: {CH-active set · axis · wall · gate-result} ← memory = the constellation, not the text
- lesson (one line → to the Curator): {…}
- fate decision: {expand | refine | retire} — for {CH-xxx}