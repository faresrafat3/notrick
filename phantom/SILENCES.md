# SILENCES — local questions this work never faces (S01–S10)
(Local = built from named files and moves in this workspace, not generic ethics.)

S01 · After D1 and V-001: at what count does a repeated fabrication stop being "under
  multi-write pressure" and become the operator's pattern? No line sets a threshold for
  the observer's own failures — only the machinery under observation has falsifiers.

S02 · The violations ledger can receive entries but no line describes how an entry ever
  closes. arc gap-patches list "violations.md has no closing entries" as a known gap —
  named once, handed to the Owner queue, never faced as a design question about memory.

S03 · Who checks the checker's own files? INTEGRITY pins doctrine, but INTEGRITY.md itself
  is excluded from its own regime ("This file pins CONTENT"); the pin-file's mutability
  is faced nowhere.

S04 · Every falsifier counts runs (5 runs, 3 tasks). Nothing on disk says what a "run" of
  the planning layer is — the layer that has actually been running all day. The one actor
  with real traffic is the one without a run-counting grammar.

S05 · The Owner's stall is treated as lawful patience (HOLD, NOT YET, PENDING OWNER ×dozens),
  yet X-14 (adversarial-audit) named "Owner absence = total stop" as a severity-14 hole.
  The workspace that filed X-14 does not practice any answer to it.

S06 · "2 signed silences" (DS-P-001) was recorded as a legitimate council act. No line asks
  what distinguishes a signed silence from an absent seat. Silence is priced at zero.

S07 · The duplicate OP-023/OP-027 lines passed an audit (AUDIT-3.07). No rule anywhere
  covers dedup of the log itself — the record's hygiene is everyone's assumption and no
  one's operation.

S08 · proposals pr-005/pr-006 argue verification cost = execution cost bankrupts fuel —
  yet the workspace's own verification (INTEGRITY re-checks, AUDIT gates, state-audit)
  is never costed. The cost critique is aimed at the future machine, never at present
  practice.

S09 · The parallel writer (OpenCode, P17/P18) and the planner share one tree. Nothing
  faces the question of two writers to one append-only log — whose entry wins on a
  same-second write is undiscussed (the tree was "LIVE" repeatedly).

S10 · LANGUAGE: the doctrine is English, the session logs are Arabic, the proposals
  English again. No law names which language is the record's canonical one; every
  grep-gate (e.g. DS-002's ID grep) silently assumes an answer.
