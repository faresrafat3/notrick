# EXCESS — machinery named then unused, or used once as costume (X01–X08)

X01 · MIC-11 id-formatter — named in CONTEXT, RULES-adjacent law, ONBOARDING, closeout
  (with a whole "decoy trap" sub-plot) — has never fired on any file on disk. All IDs
  (CH/RJ/DS/MIC/P/R/V/X/A-) exist by manual care, never by a card's act.

X02 · MIC-07 budget-counter — the closeout budget (DS-002: "20000 tokens / 30 min /
  3 attempts") exists as text; no echo line ("MIC-07 echo:") appears in any log on disk.
  The counter has counted nothing.

X03 · MIC-31 axis-tagger — R3's "axis ledger feeds the Prime's next angle" presumes
  tagged axes; zero axis tags exist anywhere in the read corpus. The ledger it feeds
  has never received a meal.

X04 · templates/RJ.md (the ruling template) — indexed in FOUNDATIONAL-BRIEF, praised in
  the cross-exam, "template-v2 pass" drafted around it — yet every ruling that ever
  happened (RJ-001) predates it; the template has never shaped a document.

X05 · The council's four seats — CH-002's layer is quoted in every rehearsal description,
  but its only real-world outputs on disk are "2 objections, 2 signed silences" in one
  chat exercise, and pr-005's "Self-red-team held" — a one-man council wearing four hats.

X06 · "K-line" — R5's mandatory result record field. No completed result record with a
  K-line exists in the workspace (DS-001 is "reserved"; DS-002 was dispatched-when-quiet
  and no DS-002 result record exists on disk). The field the Curator would refuse without
  has never been filled.

X07 · The Maker (CH-003) — its forge/reshape/prove powers are described in charter, brief,
  and audit (X-02 NO NICHE flooding); the Maker has forged nothing and received no
  documented demand. A whole charter of powers, zero invocations.

X08 · "Import Mirror" (knowledge-map) — 15 landed imports with bodies, 3 pending; the
  mirror has never been used to import anything since: no post-landing entry cites
  running the mirror process itself. The intake mechanism is finished and unemployed.
