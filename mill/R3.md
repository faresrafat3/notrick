# R3 — SURGEON reading (op 0312-0316). Fields bounded, IDs only.
| field | content |
|---|---|
| doctrine | Only what a re-runnable gate on real files can check survives; every un-runnable artifact is expendable scaffolding, and claims are false until shown. |
| object | The re-runnable gate (T0004 C0040) and its outputs — a criterion re-run verbatim, quoting the result (MIC-23 pattern). |
| discard | All non-runnable narrative: charter personal principles (Q0077 motto), IMPORTS.md bodies (name-only), DEAD_WEIGHT rows DW01-DW10, C0003/C0007/C0008 metaphors that no file executes. |
| breakthrough | A gate that runs against real workspace files and returns PASS/FAIL with zero judgment, evidence cited in the verdict line (C0040). |
| self_deception | It assumes cutting is cheap; but it amputates the "mind being grown" narrative the Owner actually signed for, so a defensible surgery may be rejected on taste — and the reading does not price that (D0045). |