# NOTATION — one-line purpose: formal/math symbols present in sources, inventoried without interpretation (op 0156 — notation DOES exist).
- G — the grid store (rcvm).
- K — K-line 4-tuple (A, D, W, G) (rcvm L52-57).
- WF(K) — the well-formed predicate (rcvm L63).
- D ∈ Axis — axis slot over a "finite axis vocabulary" (never enumerated).
- W ∈ Wall — wall slot over a "wall ledger" (never instantiated).
- p — detection probability of a cheat (cross-exam L78-80).
- penalty — the price of a caught cheat (never priced).
- p × penalty > expected gain — the deterrence inequality (no numbers).
- cost(verify), cost(execute) — verification/execution cost functions (cross-exam L57-59).
- Quorum counts: 2-of-3 RJs (H1), 5 runs (H3), 3 hits (H5), ≥3 tasks (D-01 falsifier), 5 tasks (D-02), 5 runs (supervisor falsifier).
- N ≈ — council's "N judged≈unjudged outcomes" (ch-002 L26; N never defined).
- p.308 — Minsky page anchor (pinned quote).
Op 0157 not applicable: notation exists; none invented here.
