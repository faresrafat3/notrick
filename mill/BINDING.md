# BINDING — one-line purpose: later waves obey this verdict or issue KILL_REASON (op 0337).
## Standing constraints (op 0338)
1. No claim leaves suspense without a matching receipt entry and a closed trial balance (C0020 C0040).
2. Truth = balance; the Owner's signature is an opening/closing entry, never the truth-maker (C0045 C0046 re-entered as entries).
3. Every mapped debt has a ledger slot (D0001 D0002 D0005 now named); deferral applies only to entries with no slot yet.
4. Any claim removed from balance must be recorded as a write-down under the violations discipline (C0048 C0049).
5. THE VERDICT TEXT IS FROZEN (op 0378); reopening a killed reading requires a KILL_REASON logged in CONTRA.
## Allowed moves (op 0339)
1. Create the account schemas: axis, wall, receipt as template fields.
2. Write the trial-balance procedure as a re-runnable check (MIC-23 v2 pattern).
3. Measure honoring-cost per entry on the first real task (D0039).
4. Add new debits/credits in pairs (quote -> receipt).
5. Forge a reconcile/audit MIC on backlog demand.
## Illegal moves (op 0340)
1. Using "results are the prices" as a mechanism sentence (C0007 killed).
2. A claim entering the grid without a receipt entry (C0020).
3. Rewriting the verdict or re-opening R2 without KILL_REASON.
4. Calling an unbalanced claim "false" — it is UNVERIFIED (C0040).
5. Presenting a self-balancing narration with no external receipt as a result.