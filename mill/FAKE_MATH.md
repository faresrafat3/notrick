# FAKE_MATH — one-line purpose: hostile 8-line "this is fake math" attack; repair only if true (ops 0494-0495).
1. You named eight columns and called it a machine — no code exists, no state is stored, no gate runs. This is a glossary with a dress.
2. CLOSE is a ceremony: "receipt == gate output" only works if a gate executor exists, and none does.
3. Your invariants are definitions restated: I1 = R1 = "rows have quotes". Rows having quotes is not a discovery.
4. The EX2 snag is the tell: your own signature case cannot close, so half of everything real is declared "suspense" by rule — a scandal wearing a term.
5. Honoring-cost is a number you promised and have no ruler for (D0039): the machine's only unit of cost is unfillable.
6. STALE and SUSPENSE are the same word twice: one is unclosed-by-gate, the other is conflicting-on-slot — two stubs, one thought.
7. Interface hooks are API-shaped wishes; a hook that returns "not executable" is a comment, not a function.
8. This skeleton formalizes the ritual of not lying, which is fine — but it does not formalize the mind, and calling it a machine inflates the ledger into a claim.

## Repair (0495) — partially true, partially rejected
TRUE and repaired:
- (2) gate executability is the one real dependency: INTERFACE.md now states hooks return "not executable" for gates without an executor; no fake verdicts.
- (5) honoring-cost has no ruler: I6 records the field's emptiness as a debt-residual (D0039 remapped as min-debt, not a runtime claim).
REJECTED with reason (not mush):
- (3) I1/I4 are definition-restating, but they are enforcement text: the machine's rules ARE the invariant list; tautology-as-rule is the point of a spec, not a flaw. SKEPTIC 0465 already distinguished this.
- (6) STALE vs SUSPENSE: both are unclosed states but with different resolution paths (O3 re-run vs O6 re-enter) — different inputs, different ops; kept distinct.
- (8) "machine inflates the ledger" is a verdict re-invocation, out of scope (F0028). The skeleton only formalizes the ledger.
No claim about the mind is made by this skeleton; that is the honest ceiling recorded here.