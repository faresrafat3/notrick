# EARNED_PATH — one-line purpose: the next formal objects the verdict requires (op 0346).
1. RECEIPT_SCHEMA: the credit entry — hash/algo/sample/time fields, per line, template-locked (C0020).
2. TRIAL_BALANCE: the re-runnable close — MIC-23 v2, runs the gate and checks the receipt match (C0040).
3. CHARTS_OF_ACCOUNTS: axis list (D0001) and wall list (D0002) as pinned vocabularies.
4. WRITE_DOWN_RECORD: violations.md v2 — every removed claim gets a ledger line, never a silent fix (C0048 C0049).
5. PERIOD_CLOSE: the first-run ceremony where DS records are balanced and lessons are posted (D0047).
6. STALE_PAGE_RULE: any record pair in conflict suspends both until one is re-entered (D0044).
Object set feeds PRIMITIVE_HUNT_LIST.md (0384); none is defined yet, only named.