# ENGINE — one-line purpose: v0 statement of the actual generative engine, claim IDs only (op 0246).
1. C0021 — the falsifier mandate generates every D-row (decisions carry alternatives + kill-switch).
2. C0030 + C0016 — demand-gated birth generates charters only on documented NO NICHE/Owner demand.
3. C0025 — unowned gates generate the verification structure (no claimant owns its own verdict).
4. C0036 + C0048 — hash pins + the recorded violations generate the integrity loop (V-001 proved the record can hold a proof).
5. C0046 — the signed authority chain generates lawful-change events (approved-docs moments).
6. C0039 + C0043 — the written-go requirement generates the hold mechanism (nothing runs early).
7. C0040 — a gate output contradicting a claim generates refusal (UNVERIFIED).
8. What the engine does NOT generate: prices (D0005), walls (D0002), axes (D0001), lessons (D0003), heirs (D0004) — those five are unpurchased slots, not mechanisms.