# PRESSURE — one-line purpose: 7 unpaid problems ranked by collapse x novelty (ops 0247-0254).
| rank | ID | problem | collapse 1-5 | novelty-if-paid 1-5 |
|---|---|---|---|---|
| 1 | D0005/D0044/D0045 | prices, fate, and the version conflict — the authority stack | 5 | 5 |
| 2 | D0002 | wall ledger + slot W | 5 | 4 |
| 3 | D0009/D0039 | verification cost and cheat-class penalties | 5 | 4 |
| 4 | D0001 | axis vocabulary enumeration | 4 | 4 |
| 5 | D0046 | external wall for self-attested burrows (X0004) | 4 | 4 |
| 6 | D0006 | WF = R5+R6+R10 derivation | 4 | 3 |
| 7 | D0050 | independent sampling of confessed output | 4 | 2 |

## Problem 1 (rank 1) — 6-line specification
1. Price mechanism (D0005) and fate authority (D0045) are one problem: who or what converts a result into an allocation.
2. The version conflict (D0044) infects every gate computation — no other payment may cite a gate until it is settled.
3. Resolution order: D0044 first (bookkeeping), then D0045 (authority), then D0005 (mechanism) — or the reverse only by ruling.
4. Outputs: one corrected version record; one fate-authority sentence; one checkable price noun.
5. Owner-owned decisively (R8); the mill may only frame the menu and the audit trail.
6. Done when no reading of the sources determines fate two ways, and gate arithmetic cites one version.

## Problem 2 (rank 2) — 6-line specification
1. Name the ledger file and its schema (wall-id, named constraint, owner, first stuck attempt, axis tried).
2. Define the entry rules: no retroactive walls; entries cite a DS attempt line.
3. Bind K-line slot W to a wall-id, making WF(K) checkable for slot W.
4. Wire MIC-31/Prime triggers to ledger events instead of vibes.
5. Add the file to READ_WINDOW; future ops may cite rows.
6. Done when a wall-break claim is verifiable against a row on disk.

## Problem 3 (rank 3) — 6-line specification
1. Enumerate cheat classes (fabrication, silent repair, unproven gate, stolen win).
2. For each: detection observation + cost of running it (minutes, fuel, tokens).
3. Price each penalty per class in the same unit as expected gain (D0009).
4. Measure honoring-cost on the first 3 claims or kill the field (D0039).
5. Write UNPRICED rows openly where no number is honest.
6. Done when cost(verify) and penalty numbers exist with real units and a gap analysis.

## Problem 4 (rank 4) — 6-line specification
1. Extract every axis name that already occurs in workspace text.
2. Deduplicate; each axis gets a one-sentence definition and an accept/reject example tag.
3. Pin the list where MIC-31 and rcvm can cite it; D ∈ Axis becomes checkable.
4. Define the collision rule (3 same tags → theme/promotion) against the pinned list.
5. Reject off-list tags without debate.
6. Done when an attempt tag can be rejected as off-list by a checker with no judgment.

## Problem 5 (rank 5) — 6-line specification
1. Accept C0014: self-attestation is not a wall.
2. Name one external, non-self-attested check applicable at zero day (e.g., an Owner-named supervisor re-running the boot gate).
3. Make that check mechanically recorded (verbatim quote in a file the checked agent cannot write).
4. Give it a falsifier equivalent (violations caught vs false accusations, per SUPERVISOR-CHIP).
5. Extend it to charters: burrow boundaries must cite a DS map, never self-description (C0010 anchor).
6. Done when every "who I am not" line has an external artifact checking it.

## Problem 6 (rank 6) — 6-line specification
1. Quote R5, R6, R10 clauses verbatim from RULES.md.
2. Quote the WF predicate clauses verbatim from rcvm.md.
3. Pair each WF clause to the rule clause it implements; mark unpaired.
4. Have a second checker verify the pairing independently (no self-attestation).
5. Extend or narrow honestly where clauses do not pair.
6. Done when an independent reader confirms the equivalence from the files alone.

## Problem 7 (rank 7) — 6-line specification
1. Accept U0004: self-reported output is unverified by definition.
2. Define a sampling rule: after claim, select N records blindly (seeded, not pre-announced).
3. Re-run the cheapest check on each sample (gate line re-run, hash, count).
4. Record results in a ledger; do not correct silently (violations discipline).
5. Report a mismatch rate per claimer — the only price signal a record system can own.
6. Done when the first sample batch has a number, not a promise.